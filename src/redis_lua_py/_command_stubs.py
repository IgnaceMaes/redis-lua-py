"""A typed method for every Redis command a script body can call.

Generated by scripts/generate_commands.py from Redis 8.10.1. Do not edit.

Nothing imports this at runtime. Type checkers see the ``redis`` namespace as
a subclass of ``RedisCommands``, so an editor can show each command's syntax
and documentation on hover, while the compiler goes on resolving the
namespace by value.
"""

from __future__ import annotations

from typing import Any


class RedisCommands:
    """The Redis commands, as ``redis.<name>(...)`` spells them."""

    def acl_cat(self, *args: Any) -> Any:
        """Lists the ACL categories, or the commands inside a category.

        Syntax::

            ACL CAT [category]

        Reply, one of:

        - In case ``category`` was not given, a list of existing ACL categories
        - In case ``category`` was given, list of commands that fall under the provided ACL
          category

        Time complexity: O(1) since the categories and commands are a fixed set.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'CAT', ...)``.

        https://redis.io/docs/latest/commands/acl-cat/
        """

    def acl_deluser(self, username: Any, /, *args: Any) -> Any:
        """Deletes ACL users, and terminates their connections.

        Syntax::

            ACL DELUSER username [username ...]

        Reply: The number of users that were deleted

        Time complexity: O(1) amortized time considering the typical user.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'DELUSER', ...)``.

        https://redis.io/docs/latest/commands/acl-deluser/
        """

    def acl_dryrun(self, username: Any, command: Any, /, *args: Any) -> Any:
        """Simulates the execution of a command by a user, without executing the command.

        Syntax::

            ACL DRYRUN username command [arg [arg ...]]

        Reply, one of:

        - The given user may successfully execute the given command.
        - The description of the problem, in case the user is not allowed to run the given
          command.

        Time complexity: O(1).

        Available since Redis 7.0.0. Compiles to ``redis.call('ACL', 'DRYRUN', ...)``.

        https://redis.io/docs/latest/commands/acl-dryrun/
        """

    def acl_genpass(self, *args: Any) -> Any:
        """Generates a pseudorandom, secure password that can be used to identify ACL users.

        Syntax::

            ACL GENPASS [bits]

        Reply: Pseudorandom data. By default it contains 64 bytes, representing 256 bits of
        data. If ``bits`` was given, the output string length is the number of specified bits
        (rounded to the next multiple of 4) divided by 4.

        Time complexity: O(1)

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'GENPASS', ...)``.

        https://redis.io/docs/latest/commands/acl-genpass/
        """

    def acl_getuser(self, username: Any, /) -> Any:
        """Lists the ACL rules of a user.

        Syntax::

            ACL GETUSER username

        Reply, one of:

        - a set of ACL rule definitions for the user
        - If user does not exist

        Time complexity: O(N). Where N is the number of password, command and pattern rules that
        the user has.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'GETUSER', ...)``.

        https://redis.io/docs/latest/commands/acl-getuser/
        """

    def acl_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            ACL HELP

        Reply: A list of subcommands and their description

        Time complexity: O(1)

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'HELP')``.

        https://redis.io/docs/latest/commands/acl-help/
        """

    def acl_list(self) -> Any:
        """Dumps the effective rules in ACL file format.

        Syntax::

            ACL LIST

        Reply: A list of currently active ACL rules

        Time complexity: O(N). Where N is the number of configured users.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'LIST')``.

        https://redis.io/docs/latest/commands/acl-list/
        """

    def acl_load(self) -> Any:
        """Reloads the rules from the configured ACL file.

        Syntax::

            ACL LOAD

        Time complexity: O(N). Where N is the number of configured users.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'LOAD')``.

        https://redis.io/docs/latest/commands/acl-load/
        """

    def acl_log(self, *args: Any) -> Any:
        """Lists recent security events generated due to ACL rules.

        Syntax::

            ACL LOG [count | RESET]

        Reply, one of:

        - In case ``RESET`` was not given, a list of recent ACL security events.
        - In case ``RESET`` was given, OK indicates ACL log was cleared.

        Time complexity: O(N) with N being the number of entries shown.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'LOG', ...)``.

        https://redis.io/docs/latest/commands/acl-log/
        """

    def acl_save(self) -> Any:
        """Saves the effective ACL rules in the configured ACL file.

        Syntax::

            ACL SAVE

        Time complexity: O(N). Where N is the number of configured users.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'SAVE')``.

        https://redis.io/docs/latest/commands/acl-save/
        """

    def acl_setuser(self, username: Any, /, *args: Any) -> Any:
        """Creates and modifies an ACL user and its rules.

        Syntax::

            ACL SETUSER username [rule [rule ...]]

        Time complexity: O(N). Where N is the number of rules provided.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'SETUSER', ...)``.

        https://redis.io/docs/latest/commands/acl-setuser/
        """

    def acl_users(self) -> Any:
        """Lists all ACL users.

        Syntax::

            ACL USERS

        Reply: List of existing ACL users

        Time complexity: O(N). Where N is the number of configured users.

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'USERS')``.

        https://redis.io/docs/latest/commands/acl-users/
        """

    def acl_whoami(self) -> Any:
        """Returns the authenticated username of the current connection.

        Syntax::

            ACL WHOAMI

        Reply: The username of the current connection.

        Time complexity: O(1)

        Available since Redis 6.0.0. Compiles to ``redis.call('ACL', 'WHOAMI')``.

        https://redis.io/docs/latest/commands/acl-whoami/
        """

    def append(self, key: Any, value: Any, /) -> Any:
        """Appends a string to the value of a key. Creates the key if it doesn't exist.

        Syntax::

            APPEND key value

        Reply: The length of the string after the append operation.

        Time complexity: O(1). The amortized time complexity is O(1) assuming the appended value
        is small and the already present value is of any size, since the dynamic string library
        used by Redis will double the free space available on every reallocation.

        Available since Redis 2.0.0. Compiles to ``redis.call('APPEND', ...)``.

        https://redis.io/docs/latest/commands/append/
        """

    def arcount(self, key: Any, /) -> Any:
        """Returns the number of non-empty elements in an array.

        Syntax::

            ARCOUNT key

        Reply: The number of non-empty elements, or 0 if key does not exist.

        Time complexity: O(1)

        Available since Redis 8.8.0. Compiles to ``redis.call('ARCOUNT', ...)``.

        https://redis.io/docs/latest/commands/arcount/
        """

    def ardel(self, key: Any, index: Any, /, *args: Any) -> Any:
        """Deletes elements at the specified indices in an array.

        Syntax::

            ARDEL key index [index ...]

        Reply: Number of elements deleted.

        Time complexity: O(N) where N is the number of indices to delete

        Available since Redis 8.8.0. Compiles to ``redis.call('ARDEL', ...)``.

        https://redis.io/docs/latest/commands/ardel/
        """

    def ardelrange(self, key: Any, /, *args: Any) -> Any:
        """Deletes elements in one or more ranges.

        Syntax::

            ARDELRANGE key start end [start end ...]

        Reply: Number of elements deleted.

        Time complexity: Proportional to the number of existing elements / slices touched, not
        to the numeric span of the requested ranges

        Available since Redis 8.8.0. Compiles to ``redis.call('ARDELRANGE', ...)``.

        https://redis.io/docs/latest/commands/ardelrange/
        """

    def arget(self, key: Any, index: Any, /) -> Any:
        """Gets the value at an index in an array.

        Syntax::

            ARGET key index

        Reply, one of:

        - The value at the given index.
        - Null reply if key or index does not exist.

        Time complexity: O(1)

        Available since Redis 8.8.0. Compiles to ``redis.call('ARGET', ...)``.

        https://redis.io/docs/latest/commands/arget/
        """

    def argetrange(self, key: Any, start: Any, end: Any, /) -> Any:
        """Gets values in a range of indices.

        Syntax::

            ARGETRANGE key start end

        Time complexity: O(N) where N is the range length

        Available since Redis 8.8.0. Compiles to ``redis.call('ARGETRANGE', ...)``.

        https://redis.io/docs/latest/commands/argetrange/
        """

    def argrep(self, key: Any, start: Any, end: Any, /, *args: Any) -> Any:
        """Searches array elements in a range using textual predicates.

        Syntax::

            ARGREP key start end <EXACT string | MATCH string | GLOB pattern | RE pattern>
                [<EXACT string | MATCH string | GLOB pattern | RE pattern> ...] [<AND | OR |
                LIMIT limit | WITHVALUES | NOCASE> [<AND | OR | LIMIT limit | WITHVALUES |
                NOCASE> ...]]

        Reply, one of:

        - Array of matching indexes.
        - Array of [index, value] pairs. Returned in case ``WITHVALUES`` was used.

        Time complexity: O(P * C) where P is the number of visited positions in touched slices
        and C is the cost of evaluating the predicates on one existing element.

        Available since Redis 8.8.0. Compiles to ``redis.call('ARGREP', ...)``.

        https://redis.io/docs/latest/commands/argrep/
        """

    def arinfo(self, key: Any, /, *args: Any) -> Any:
        """Returns metadata about an array.

        Syntax::

            ARINFO key [FULL]

        Time complexity: O(1), or O(N) with FULL option where N is the number of slices.

        Available since Redis 8.8.0. Compiles to ``redis.call('ARINFO', ...)``.

        https://redis.io/docs/latest/commands/arinfo/
        """

    def arinsert(self, key: Any, value: Any, /, *args: Any) -> Any:
        """Inserts one or more values at consecutive indices.

        Syntax::

            ARINSERT key value [value ...]

        Reply: The last index where a value was inserted.

        Time complexity: O(N) where N is the number of values

        Available since Redis 8.8.0. Compiles to ``redis.call('ARINSERT', ...)``.

        https://redis.io/docs/latest/commands/arinsert/
        """

    def arlastitems(self, key: Any, count: Any, /, *args: Any) -> Any:
        """Returns the most recently inserted elements.

        Syntax::

            ARLASTITEMS key count [REV]

        Time complexity: O(N) where N is the count

        Available since Redis 8.8.0. Compiles to ``redis.call('ARLASTITEMS', ...)``.

        https://redis.io/docs/latest/commands/arlastitems/
        """

    def arlen(self, key: Any, /) -> Any:
        """Returns the length of an array (max index + 1).

        Syntax::

            ARLEN key

        Reply: The length of the array (max index + 1), or 0 if key does not exist.

        Time complexity: O(1)

        Available since Redis 8.8.0. Compiles to ``redis.call('ARLEN', ...)``.

        https://redis.io/docs/latest/commands/arlen/
        """

    def armget(self, key: Any, index: Any, /, *args: Any) -> Any:
        """Gets values at multiple indices in an array.

        Syntax::

            ARMGET key index [index ...]

        Time complexity: O(N) where N is the number of indices

        Available since Redis 8.8.0. Compiles to ``redis.call('ARMGET', ...)``.

        https://redis.io/docs/latest/commands/armget/
        """

    def armset(self, key: Any, /, *args: Any) -> Any:
        """Sets multiple index-value pairs in an array.

        Syntax::

            ARMSET key index value [index value ...]

        Reply: Number of new slots that were set (previously empty).

        Time complexity: O(N) where N is the number of pairs

        Available since Redis 8.8.0. Compiles to ``redis.call('ARMSET', ...)``.

        https://redis.io/docs/latest/commands/armset/
        """

    def arnext(self, key: Any, /) -> Any:
        """Returns the next index ARINSERT would use.

        Syntax::

            ARNEXT key

        Reply, one of:

        - The next index ARINSERT would use. Returns 0 for missing keys or when no insert
          happened yet.
        - Null when the insertion cursor is exhausted (next insert would overflow).

        Time complexity: O(1)

        Available since Redis 8.8.0. Compiles to ``redis.call('ARNEXT', ...)``.

        https://redis.io/docs/latest/commands/arnext/
        """

    def arop(self, key: Any, start: Any, end: Any, /, *args: Any) -> Any:
        """Performs aggregate operations on array elements in a range.

        Syntax::

            AROP key start end <SUM | MIN | MAX | AND | OR | XOR | MATCH value | USED>

        Reply, one of:

        - Result of the operation.
        - Integer result for MATCH, USED, AND, OR, XOR.
        - Null if no elements match the operation.

        Time complexity: O(P) where P is visited positions in touched slices (dense scanned
        slots + sparse entries), with worst-case O(|end-start|+1) and typical case close to
        O(N), where N is the number of existing elements in range.

        Available since Redis 8.8.0. Compiles to ``redis.call('AROP', ...)``.

        https://redis.io/docs/latest/commands/arop/
        """

    def arring(self, key: Any, size: Any, value: Any, /, *args: Any) -> Any:
        """Inserts values into a ring buffer of specified size, wrapping and truncating as needed.

        Syntax::

            ARRING key size value [value ...]

        Reply: The last index where a value was inserted.

        Time complexity: O(M) normally, O(N+M) on ring resize, where N is the maximum of the old
        and new ring size and M is the number of inserted values

        Available since Redis 8.8.0. Compiles to ``redis.call('ARRING', ...)``.

        https://redis.io/docs/latest/commands/arring/
        """

    def arscan(self, key: Any, start: Any, end: Any, /, *args: Any) -> Any:
        """Iterates existing elements in a range, returning index-value pairs.

        Syntax::

            ARSCAN key start end [LIMIT limit]

        Reply: Array of [index, value] pairs.

        Time complexity: O(P) where P is visited positions in touched slices (dense scanned
        slots + sparse entries), with worst-case O(|end-start|+1) and typical case close to
        O(N), where N is the number of existing elements in range.

        Available since Redis 8.8.0. Compiles to ``redis.call('ARSCAN', ...)``.

        https://redis.io/docs/latest/commands/arscan/
        """

    def arseek(self, key: Any, index: Any, /) -> Any:
        """Sets the ARINSERT / ARRING cursor to a specific index.

        Syntax::

            ARSEEK key index

        Reply: 1 if the cursor was set, 0 if the key does not exist.

        Time complexity: O(1)

        Available since Redis 8.8.0. Compiles to ``redis.call('ARSEEK', ...)``.

        https://redis.io/docs/latest/commands/arseek/
        """

    def arset(self, key: Any, index: Any, value: Any, /, *args: Any) -> Any:
        """Sets one or more contiguous values starting at an index in an array.

        Syntax::

            ARSET key index value [value ...]

        Reply: Number of new slots that were set (previously empty).

        Time complexity: O(N) where N is the number of values

        Available since Redis 8.8.0. Compiles to ``redis.call('ARSET', ...)``.

        https://redis.io/docs/latest/commands/arset/
        """

    def asking(self) -> Any:
        """Signals that a cluster client is following an -ASK redirect.

        Syntax::

            ASKING

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('ASKING')``.

        https://redis.io/docs/latest/commands/asking/
        """

    def auth(self, *args: Any) -> Any:
        """Authenticates the connection.

        Syntax::

            AUTH [username] password

        Time complexity: O(N) where N is the number of passwords defined for the user

        Available since Redis 1.0.0. Compiles to ``redis.call('AUTH', ...)``.

        https://redis.io/docs/latest/commands/auth/
        """

    def backup_abort(self) -> Any:
        """Cancel a backup that has not been sealed yet.

        Syntax::

            BACKUP ABORT

        Time complexity: O(1)

        Available since Redis 8.10.0. Compiles to ``redis.call('BACKUP', 'ABORT')``.

        https://redis.io/docs/latest/commands/backup-abort/
        """

    def backup_cleanup(self) -> Any:
        """Remove a sealed backup's files and return to idle.

        Syntax::

            BACKUP CLEANUP

        Time complexity: O(1)

        Available since Redis 8.10.0. Compiles to ``redis.call('BACKUP', 'CLEANUP')``.

        https://redis.io/docs/latest/commands/backup-cleanup/
        """

    def backup_help(self) -> Any:
        """Return helpful text about BACKUP command parameters.

        Syntax::

            BACKUP HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 8.10.0. Compiles to ``redis.call('BACKUP', 'HELP')``.

        https://redis.io/docs/latest/commands/backup-help/
        """

    def backup_list(self) -> Any:
        """List the immutable backup file paths pinned so far.

        Syntax::

            BACKUP LIST

        Reply: The absolute paths of immutable files pinned so far.

        Time complexity: O(1)

        Available since Redis 8.10.0. Compiles to ``redis.call('BACKUP', 'LIST')``.

        https://redis.io/docs/latest/commands/backup-list/
        """

    def backup_seal(self) -> Any:
        """Freeze the current backup (BASE + INCR + manifest).

        Syntax::

            BACKUP SEAL

        Time complexity: O(1)

        Available since Redis 8.10.0. Compiles to ``redis.call('BACKUP', 'SEAL')``.

        https://redis.io/docs/latest/commands/backup-seal/
        """

    def backup_start(self) -> Any:
        """Start a new backup into the configured 'backupdirname'.

        Syntax::

            BACKUP START

        Time complexity: O(1)

        Available since Redis 8.10.0. Compiles to ``redis.call('BACKUP', 'START')``.

        https://redis.io/docs/latest/commands/backup-start/
        """

    def backup_status(self) -> Any:
        """Report the current backup state.

        Syntax::

            BACKUP STATUS

        Reply: Current backup state.

        Time complexity: O(1)

        Available since Redis 8.10.0. Compiles to ``redis.call('BACKUP', 'STATUS')``.

        https://redis.io/docs/latest/commands/backup-status/
        """

    def bgrewriteaof(self) -> Any:
        """Asynchronously rewrites the append-only file to disk.

        Syntax::

            BGREWRITEAOF

        Reply: A simple string reply indicating that the rewriting started or is about to start
        ASAP

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('BGREWRITEAOF')``.

        https://redis.io/docs/latest/commands/bgrewriteaof/
        """

    def bgsave(self, *args: Any) -> Any:
        """Asynchronously saves the database(s) to disk.

        Syntax::

            BGSAVE [SCHEDULE]

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('BGSAVE', ...)``.

        https://redis.io/docs/latest/commands/bgsave/
        """

    def bitcount(self, key: Any, /, *args: Any) -> Any:
        """Counts the number of set bits (population counting) in a string.

        Syntax::

            BITCOUNT key [start end [BYTE | BIT]]

        Reply: The number of bits set to 1.

        Time complexity: O(N)

        Available since Redis 2.6.0. Compiles to ``redis.call('BITCOUNT', ...)``.

        https://redis.io/docs/latest/commands/bitcount/
        """

    def bitfield(self, key: Any, /, *args: Any) -> Any:
        """Performs arbitrary bitfield integer operations on strings.

        Syntax::

            BITFIELD key [<GET encoding offset | [OVERFLOW <WRAP | SAT | FAIL>] <SET encoding
                offset value | INCRBY encoding offset increment>> [<GET encoding offset |
                [OVERFLOW <WRAP | SAT | FAIL>] <SET encoding offset value | INCRBY encoding
                offset increment>> ...]]

        Time complexity: O(1) for each subcommand specified

        Available since Redis 3.2.0. Compiles to ``redis.call('BITFIELD', ...)``.

        https://redis.io/docs/latest/commands/bitfield/
        """

    def bitfield_ro(self, key: Any, /, *args: Any) -> Any:
        """Performs arbitrary read-only bitfield integer operations on strings.

        Syntax::

            BITFIELD_RO key [GET encoding offset [GET encoding offset ...]]

        Time complexity: O(1) for each subcommand specified

        Available since Redis 6.0.0. Compiles to ``redis.call('BITFIELD_RO', ...)``.

        https://redis.io/docs/latest/commands/bitfield_ro/
        """

    def bitop(self, *args: Any) -> Any:
        """Performs bitwise operations on multiple strings, and stores the result.

        Syntax::

            BITOP <AND | OR | XOR | NOT | DIFF | DIFF1 | ANDOR | ONE> destkey key [key ...]

        Reply: the size of the string stored in the destination key, that is equal to the size
        of the longest input string

        Time complexity: O(N)

        Available since Redis 2.6.0. Compiles to ``redis.call('BITOP', ...)``.

        https://redis.io/docs/latest/commands/bitop/
        """

    def bitpos(self, key: Any, bit: Any, /, *args: Any) -> Any:
        """Finds the first set (1) or clear (0) bit in a string.

        Syntax::

            BITPOS key bit [start [end [BYTE | BIT]]]

        Reply, one of:

        - the position of the first bit set to 1 or 0 according to the request
        - In case the ``bit`` argument is 1 and the string is empty or composed of just zero
          bytes

        Time complexity: O(N)

        Available since Redis 2.8.7. Compiles to ``redis.call('BITPOS', ...)``.

        https://redis.io/docs/latest/commands/bitpos/
        """

    def blmove(self, source: Any, destination: Any, /, *args: Any) -> Any:
        """Pops an element from a list, pushes it to another list and returns it. Blocks until an
        element is available otherwise. Deletes the list if the last element was moved.

        Syntax::

            BLMOVE source destination <LEFT | RIGHT> <LEFT | RIGHT> timeout

        Reply, one of:

        - The popped element.
        - Operation timed-out

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('BLMOVE', ...)``.

        https://redis.io/docs/latest/commands/blmove/
        """

    def blmovem(self, source: Any, destination: Any, /, *args: Any) -> Any:
        """Moves up to (or exactly) a number of elements from one list to another and returns them.
        Blocks until the elements are available otherwise. Deletes the source list if it becomes
        empty.

        Syntax::

            BLMOVEM source destination <LEFT | RIGHT> <LEFT | RIGHT> timeout [<COUNT count |
                EXACTLY exactly> <OBO | BULK>]

        Reply, one of:

        - Operation timed-out.
        - The moved elements, in destination order.

        Time complexity: O(N) where N is the number of elements moved.

        Available since Redis 8.10.0. Compiles to ``redis.call('BLMOVEM', ...)``.

        https://redis.io/docs/latest/commands/blmovem/
        """

    def blmpop(self, timeout: Any, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Pops the first element from one of multiple lists. Blocks until an element is available
        otherwise. Deletes the list if the last element was popped.

        Syntax::

            BLMPOP timeout numkeys key [key ...] <LEFT | RIGHT> [COUNT count]

        Reply, one of:

        - Operation timed-out
        - The key from which elements were popped and the popped elements

        Time complexity: O(N+M) where N is the number of provided keys and M is the number of
        elements returned.

        Available since Redis 7.0.0. Compiles to ``redis.call('BLMPOP', ...)``.

        https://redis.io/docs/latest/commands/blmpop/
        """

    def blpop(self, key: Any, /, *args: Any) -> Any:
        """Removes and returns the first element in a list. Blocks until an element is available
        otherwise. Deletes the list if the last element was popped.

        Syntax::

            BLPOP key [key ...] timeout

        Reply, one of:

        - No element could be popped and timeout expired
        - The key from which the element was popped and the value of the popped element

        Time complexity: O(N) where N is the number of provided keys.

        Available since Redis 2.0.0. Compiles to ``redis.call('BLPOP', ...)``.

        https://redis.io/docs/latest/commands/blpop/
        """

    def brpop(self, key: Any, /, *args: Any) -> Any:
        """Removes and returns the last element in a list. Blocks until an element is available
        otherwise. Deletes the list if the last element was popped.

        Syntax::

            BRPOP key [key ...] timeout

        Reply: No element could be popped and the timeout expired.

        Time complexity: O(N) where N is the number of provided keys.

        Available since Redis 2.0.0. Compiles to ``redis.call('BRPOP', ...)``.

        https://redis.io/docs/latest/commands/brpop/
        """

    def brpoplpush(self, source: Any, destination: Any, timeout: Any, /) -> Any:
        """Pops an element from a list, pushes it to another list and returns it. Block until an
        element is available otherwise. Deletes the list if the last element was popped.

        Deprecated since Redis 6.2.0, replaced by ``BLMOVE`` with the ``RIGHT`` and ``LEFT``
        arguments.

        Syntax::

            BRPOPLPUSH source destination timeout

        Reply, one of:

        - The element being popped from source and pushed to destination.
        - Timeout is reached.

        Time complexity: O(1)

        Available since Redis 2.2.0. Compiles to ``redis.call('BRPOPLPUSH', ...)``.

        https://redis.io/docs/latest/commands/brpoplpush/
        """

    def bzmpop(self, timeout: Any, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Removes and returns a member by score from one or more sorted sets. Blocks until a
        member is available otherwise. Deletes the sorted set if the last element was popped.

        Syntax::

            BZMPOP timeout numkeys key [key ...] <MIN | MAX> [COUNT count]

        Reply, one of:

        - Timeout reached and no elements were popped.
        - The keyname and the popped members.

        Time complexity: O(K) + O(M*log(N)) where K is the number of provided keys, N being the
        number of elements in the sorted set, and M being the number of elements popped.

        Available since Redis 7.0.0. Compiles to ``redis.call('BZMPOP', ...)``.

        https://redis.io/docs/latest/commands/bzmpop/
        """

    def bzpopmax(self, key: Any, /, *args: Any) -> Any:
        """Removes and returns the member with the highest score from one or more sorted sets.
        Blocks until a member available otherwise. Deletes the sorted set if the last element
        was popped.

        Syntax::

            BZPOPMAX key [key ...] timeout

        Reply, one of:

        - Timeout reached and no elements were popped.
        - The keyname, popped member, and its score.

        Time complexity: O(log(N)) with N being the number of elements in the sorted set.

        Available since Redis 5.0.0. Compiles to ``redis.call('BZPOPMAX', ...)``.

        https://redis.io/docs/latest/commands/bzpopmax/
        """

    def bzpopmin(self, key: Any, /, *args: Any) -> Any:
        """Removes and returns the member with the lowest score from one or more sorted sets.
        Blocks until a member is available otherwise. Deletes the sorted set if the last element
        was popped.

        Syntax::

            BZPOPMIN key [key ...] timeout

        Reply, one of:

        - Timeout reached and no elements were popped.
        - The keyname, popped member, and its score.

        Time complexity: O(log(N)) with N being the number of elements in the sorted set.

        Available since Redis 5.0.0. Compiles to ``redis.call('BZPOPMIN', ...)``.

        https://redis.io/docs/latest/commands/bzpopmin/
        """

    def client_caching(self, *args: Any) -> Any:
        """Instructs the server whether to track the keys in the next request.

        Syntax::

            CLIENT CACHING <YES | NO>

        Time complexity: O(1)

        Available since Redis 6.0.0. Compiles to ``redis.call('CLIENT', 'CACHING', ...)``.

        https://redis.io/docs/latest/commands/client-caching/
        """

    def client_getname(self) -> Any:
        """Returns the name of the connection.

        Syntax::

            CLIENT GETNAME

        Reply, one of:

        - The connection name of the current connection
        - Connection name was not set

        Time complexity: O(1)

        Available since Redis 2.6.9. Compiles to ``redis.call('CLIENT', 'GETNAME')``.

        https://redis.io/docs/latest/commands/client-getname/
        """

    def client_getredir(self) -> Any:
        """Returns the client ID to which the connection's tracking notifications are redirected.

        Syntax::

            CLIENT GETREDIR

        Reply, one of:

        - Not redirecting notifications to any client.
        - Client tracking is not enabled.
        - ID of the client we are redirecting the notifications to.

        Time complexity: O(1)

        Available since Redis 6.0.0. Compiles to ``redis.call('CLIENT', 'GETREDIR')``.

        https://redis.io/docs/latest/commands/client-getredir/
        """

    def client_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            CLIENT HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('CLIENT', 'HELP')``.

        https://redis.io/docs/latest/commands/client-help/
        """

    def client_id(self) -> Any:
        """Returns the unique client ID of the connection.

        Syntax::

            CLIENT ID

        Reply: The id of the client

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('CLIENT', 'ID')``.

        https://redis.io/docs/latest/commands/client-id/
        """

    def client_info(self) -> Any:
        """Returns information about the connection.

        Syntax::

            CLIENT INFO

        Reply: a unique string, as described at the CLIENT LIST page, for the current client

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('CLIENT', 'INFO')``.

        https://redis.io/docs/latest/commands/client-info/
        """

    def client_kill(self, *args: Any) -> Any:
        """Terminates open connections.

        Syntax::

            CLIENT KILL <ip:port | <[ID client-id] | [TYPE <normal | master | slave | replica |
                pubsub>] | [USER username] | [ADDR ip:port] | [LADDR ip:port] | [SKIPME <YES |
                NO>] | [MAXAGE maxage]> [<[ID client-id] | [TYPE <normal | master | slave |
                replica | pubsub>] | [USER username] | [ADDR ip:port] | [LADDR ip:port] |
                [SKIPME <YES | NO>] | [MAXAGE maxage]> ...]>

        Reply, one of:

        - when called in 3 argument format
        - when called in filter/value format, the number of clients killed

        Time complexity: O(N) where N is the number of client connections

        Available since Redis 2.4.0. Compiles to ``redis.call('CLIENT', 'KILL', ...)``.

        https://redis.io/docs/latest/commands/client-kill/
        """

    def client_list(self, *args: Any) -> Any:
        """Lists open connections.

        Syntax::

            CLIENT LIST [TYPE <normal | master | replica | pubsub>] [ID client-id [client-id
                ...]]

        Reply: Information and statistics about client connections

        Time complexity: O(N) where N is the number of client connections

        Available since Redis 2.4.0. Compiles to ``redis.call('CLIENT', 'LIST', ...)``.

        https://redis.io/docs/latest/commands/client-list/
        """

    def client_no_evict(self, *args: Any) -> Any:
        """Sets the client eviction mode of the connection.

        Syntax::

            CLIENT NO-EVICT <ON | OFF>

        Time complexity: O(1)

        Available since Redis 7.0.0. Compiles to ``redis.call('CLIENT', 'NO-EVICT', ...)``.

        https://redis.io/docs/latest/commands/client-no-evict/
        """

    def client_no_touch(self, *args: Any) -> Any:
        """Controls whether commands sent by the client affect the LRU/LFU of accessed keys.

        Syntax::

            CLIENT NO-TOUCH <ON | OFF>

        Time complexity: O(1)

        Available since Redis 7.2.0. Compiles to ``redis.call('CLIENT', 'NO-TOUCH', ...)``.

        https://redis.io/docs/latest/commands/client-no-touch/
        """

    def client_pause(self, timeout: Any, /, *args: Any) -> Any:
        """Suspends commands processing.

        Syntax::

            CLIENT PAUSE timeout [WRITE | ALL]

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLIENT', 'PAUSE', ...)``.

        https://redis.io/docs/latest/commands/client-pause/
        """

    def client_reply(self, *args: Any) -> Any:
        """Instructs the server whether to reply to commands.

        Syntax::

            CLIENT REPLY <ON | OFF | SKIP>

        Reply: When called with either OFF or SKIP subcommands, no reply is made. When called
        with ON, reply is OK.

        Time complexity: O(1)

        Available since Redis 3.2.0. Compiles to ``redis.call('CLIENT', 'REPLY', ...)``.

        https://redis.io/docs/latest/commands/client-reply/
        """

    def client_setinfo(self, *args: Any) -> Any:
        """Sets information specific to the client or connection.

        Syntax::

            CLIENT SETINFO <lib-name libname | lib-ver libver>

        Time complexity: O(1)

        Available since Redis 7.2.0. Compiles to ``redis.call('CLIENT', 'SETINFO', ...)``.

        https://redis.io/docs/latest/commands/client-setinfo/
        """

    def client_setname(self, connection_name: Any, /) -> Any:
        """Sets the connection name.

        Syntax::

            CLIENT SETNAME connection-name

        Time complexity: O(1)

        Available since Redis 2.6.9. Compiles to ``redis.call('CLIENT', 'SETNAME', ...)``.

        https://redis.io/docs/latest/commands/client-setname/
        """

    def client_tracking(self, *args: Any) -> Any:
        """Controls server-assisted client-side caching for the connection.

        Syntax::

            CLIENT TRACKING <ON | OFF> [REDIRECT client-id] [PREFIX prefix [PREFIX prefix ...]]
                [BCAST] [OPTIN] [OPTOUT] [NOLOOP]

        Reply: if the client was successfully put into or taken out of tracking mode

        Time complexity: O(1). Some options may introduce additional complexity.

        Available since Redis 6.0.0. Compiles to ``redis.call('CLIENT', 'TRACKING', ...)``.

        https://redis.io/docs/latest/commands/client-tracking/
        """

    def client_trackinginfo(self) -> Any:
        """Returns information about server-assisted client-side caching for the connection.

        Syntax::

            CLIENT TRACKINGINFO

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('CLIENT', 'TRACKINGINFO')``.

        https://redis.io/docs/latest/commands/client-trackinginfo/
        """

    def client_unblock(self, client_id: Any, /, *args: Any) -> Any:
        """Unblocks a client blocked by a blocking command from a different connection.

        Syntax::

            CLIENT UNBLOCK client-id [TIMEOUT | ERROR]

        Reply, one of:

        - if the client was unblocked successfully
        - if the client wasn't unblocked

        Time complexity: O(log N) where N is the number of client connections

        Available since Redis 5.0.0. Compiles to ``redis.call('CLIENT', 'UNBLOCK', ...)``.

        https://redis.io/docs/latest/commands/client-unblock/
        """

    def client_unpause(self) -> Any:
        """Resumes processing commands from paused clients.

        Syntax::

            CLIENT UNPAUSE

        Time complexity: O(N) Where N is the number of paused clients

        Available since Redis 6.2.0. Compiles to ``redis.call('CLIENT', 'UNPAUSE')``.

        https://redis.io/docs/latest/commands/client-unpause/
        """

    def cluster_addslots(self, slot: Any, /, *args: Any) -> Any:
        """Assigns new hash slots to a node.

        Syntax::

            CLUSTER ADDSLOTS slot [slot ...]

        Time complexity: O(N) where N is the total number of hash slot arguments

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'ADDSLOTS', ...)``.

        https://redis.io/docs/latest/commands/cluster-addslots/
        """

    def cluster_addslotsrange(self, *args: Any) -> Any:
        """Assigns new hash slot ranges to a node.

        Syntax::

            CLUSTER ADDSLOTSRANGE start-slot end-slot [start-slot end-slot ...]

        Time complexity: O(N) where N is the total number of the slots between the start slot
        and end slot arguments.

        Available since Redis 7.0.0. Compiles to ``redis.call('CLUSTER', 'ADDSLOTSRANGE',
        ...)``.

        https://redis.io/docs/latest/commands/cluster-addslotsrange/
        """

    def cluster_bumpepoch(self) -> Any:
        """Advances the cluster config epoch.

        Syntax::

            CLUSTER BUMPEPOCH

        Reply, one of:

        - if the epoch was incremented
        - if the node already has the greatest config epoch in the cluster

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'BUMPEPOCH')``.

        https://redis.io/docs/latest/commands/cluster-bumpepoch/
        """

    def cluster_count_failure_reports(self, node_id: Any, /) -> Any:
        """Returns the number of active failure reports active for a node.

        Syntax::

            CLUSTER COUNT-FAILURE-REPORTS node-id

        Reply: the number of active failure reports for the node

        Time complexity: O(N) where N is the number of failure reports

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'COUNT-FAILURE-
        REPORTS', ...)``.

        https://redis.io/docs/latest/commands/cluster-count-failure-reports/
        """

    def cluster_countkeysinslot(self, slot: Any, /) -> Any:
        """Returns the number of keys in a hash slot.

        Syntax::

            CLUSTER COUNTKEYSINSLOT slot

        Reply: The number of keys in the specified hash slot

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'COUNTKEYSINSLOT',
        ...)``.

        https://redis.io/docs/latest/commands/cluster-countkeysinslot/
        """

    def cluster_delslots(self, slot: Any, /, *args: Any) -> Any:
        """Sets hash slots as unbound for a node.

        Syntax::

            CLUSTER DELSLOTS slot [slot ...]

        Time complexity: O(N) where N is the total number of hash slot arguments

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'DELSLOTS', ...)``.

        https://redis.io/docs/latest/commands/cluster-delslots/
        """

    def cluster_delslotsrange(self, *args: Any) -> Any:
        """Sets hash slot ranges as unbound for a node.

        Syntax::

            CLUSTER DELSLOTSRANGE start-slot end-slot [start-slot end-slot ...]

        Time complexity: O(N) where N is the total number of the slots between the start slot
        and end slot arguments.

        Available since Redis 7.0.0. Compiles to ``redis.call('CLUSTER', 'DELSLOTSRANGE',
        ...)``.

        https://redis.io/docs/latest/commands/cluster-delslotsrange/
        """

    def cluster_failover(self, *args: Any) -> Any:
        """Forces a replica to perform a manual failover of its master.

        Syntax::

            CLUSTER FAILOVER [FORCE | TAKEOVER]

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'FAILOVER', ...)``.

        https://redis.io/docs/latest/commands/cluster-failover/
        """

    def cluster_flushslots(self) -> Any:
        """Deletes all slots information from a node.

        Syntax::

            CLUSTER FLUSHSLOTS

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'FLUSHSLOTS')``.

        https://redis.io/docs/latest/commands/cluster-flushslots/
        """

    def cluster_forget(self, node_id: Any, /) -> Any:
        """Removes a node from the nodes table.

        Syntax::

            CLUSTER FORGET node-id

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'FORGET', ...)``.

        https://redis.io/docs/latest/commands/cluster-forget/
        """

    def cluster_getkeysinslot(self, slot: Any, count: Any, /) -> Any:
        """Returns the key names in a hash slot.

        Syntax::

            CLUSTER GETKEYSINSLOT slot count

        Reply: an array with up to count elements

        Time complexity: O(N) where N is the number of requested keys

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'GETKEYSINSLOT',
        ...)``.

        https://redis.io/docs/latest/commands/cluster-getkeysinslot/
        """

    def cluster_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            CLUSTER HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('CLUSTER', 'HELP')``.

        https://redis.io/docs/latest/commands/cluster-help/
        """

    def cluster_info(self) -> Any:
        """Returns information about the state of a node.

        Syntax::

            CLUSTER INFO

        Reply: A map between named fields and values in the form of <field>:<value> lines
        separated by newlines composed by the two bytes CRLF

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'INFO')``.

        https://redis.io/docs/latest/commands/cluster-info/
        """

    def cluster_keyslot(self, key: Any, /) -> Any:
        """Returns the hash slot for a key.

        Syntax::

            CLUSTER KEYSLOT key

        Reply: The hash slot number for the specified key

        Time complexity: O(N) where N is the number of bytes in the key

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'KEYSLOT', ...)``.

        https://redis.io/docs/latest/commands/cluster-keyslot/
        """

    def cluster_links(self) -> Any:
        """Returns a list of all TCP links to and from peer nodes.

        Syntax::

            CLUSTER LINKS

        Reply: an array of cluster links and their attributes

        Time complexity: O(N) where N is the total number of Cluster nodes

        Available since Redis 7.0.0. Compiles to ``redis.call('CLUSTER', 'LINKS')``.

        https://redis.io/docs/latest/commands/cluster-links/
        """

    def cluster_meet(self, ip: Any, port: Any, /, *args: Any) -> Any:
        """Forces a node to handshake with another node.

        Syntax::

            CLUSTER MEET ip port [cluster-bus-port]

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'MEET', ...)``.

        https://redis.io/docs/latest/commands/cluster-meet/
        """

    def cluster_migration(self, *args: Any) -> Any:
        """Start, monitor and cancel slot migration.

        Syntax::

            CLUSTER MIGRATION <IMPORT start-slot end-slot [start-slot end-slot ...] | CANCEL <ID
                task-id | ALL> | STATUS <[ID task-id] | [ALL]>>

        Reply, one of:

        - Reply to CLUSTER MIGRATION IMPORT, returns the task ID.
        - Reply to CLUSTER MIGRATION CANCEL, number of cancelled migration operations.
        - Reply to CLUSTER MIGRATION STATUS, array of migration operation details.

        Time complexity: O(N) where N is the total number of the slots between the start slot
        and end slot arguments.

        Available since Redis 8.4.0. Compiles to ``redis.call('CLUSTER', 'MIGRATION', ...)``.

        https://redis.io/docs/latest/commands/cluster-migration/
        """

    def cluster_myid(self) -> Any:
        """Returns the ID of a node.

        Syntax::

            CLUSTER MYID

        Reply: the node id

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'MYID')``.

        https://redis.io/docs/latest/commands/cluster-myid/
        """

    def cluster_myshardid(self) -> Any:
        """Returns the shard ID of a node.

        Syntax::

            CLUSTER MYSHARDID

        Reply: the node's shard id

        Time complexity: O(1)

        Available since Redis 7.2.0. Compiles to ``redis.call('CLUSTER', 'MYSHARDID')``.

        https://redis.io/docs/latest/commands/cluster-myshardid/
        """

    def cluster_nodes(self) -> Any:
        """Returns the cluster configuration for a node.

        Syntax::

            CLUSTER NODES

        Reply: the serialized cluster configuration

        Time complexity: O(N) where N is the total number of Cluster nodes

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'NODES')``.

        https://redis.io/docs/latest/commands/cluster-nodes/
        """

    def cluster_replicas(self, node_id: Any, /) -> Any:
        """Lists the replica nodes of a master node.

        Syntax::

            CLUSTER REPLICAS node-id

        Reply: a list of replica nodes replicating from the specified master node provided in
        the same format used by CLUSTER NODES

        Time complexity: O(N) where N is the number of replicas.

        Available since Redis 5.0.0. Compiles to ``redis.call('CLUSTER', 'REPLICAS', ...)``.

        https://redis.io/docs/latest/commands/cluster-replicas/
        """

    def cluster_replicate(self, node_id: Any, /) -> Any:
        """Configure a node as replica of a master node.

        Syntax::

            CLUSTER REPLICATE node-id

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'REPLICATE', ...)``.

        https://redis.io/docs/latest/commands/cluster-replicate/
        """

    def cluster_reset(self, *args: Any) -> Any:
        """Resets a node.

        Syntax::

            CLUSTER RESET [HARD | SOFT]

        Time complexity: O(N) where N is the number of known nodes. The command may execute a
        FLUSHALL as a side effect.

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'RESET', ...)``.

        https://redis.io/docs/latest/commands/cluster-reset/
        """

    def cluster_saveconfig(self) -> Any:
        """Forces a node to save the cluster configuration to disk.

        Syntax::

            CLUSTER SAVECONFIG

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'SAVECONFIG')``.

        https://redis.io/docs/latest/commands/cluster-saveconfig/
        """

    def cluster_set_config_epoch(self, config_epoch: Any, /) -> Any:
        """Sets the configuration epoch for a new node.

        Syntax::

            CLUSTER SET-CONFIG-EPOCH config-epoch

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'SET-CONFIG-EPOCH',
        ...)``.

        https://redis.io/docs/latest/commands/cluster-set-config-epoch/
        """

    def cluster_setslot(self, slot: Any, /, *args: Any) -> Any:
        """Binds a hash slot to a node.

        Syntax::

            CLUSTER SETSLOT slot <IMPORTING node-id | MIGRATING node-id | NODE node-id | STABLE>

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'SETSLOT', ...)``.

        https://redis.io/docs/latest/commands/cluster-setslot/
        """

    def cluster_shards(self) -> Any:
        """Returns the mapping of cluster slots to shards.

        Syntax::

            CLUSTER SHARDS

        Reply: a nested list of a map of hash ranges and shard nodes describing individual
        shards

        Time complexity: O(N) where N is the total number of cluster nodes

        Available since Redis 7.0.0. Compiles to ``redis.call('CLUSTER', 'SHARDS')``.

        https://redis.io/docs/latest/commands/cluster-shards/
        """

    def cluster_slaves(self, node_id: Any, /) -> Any:
        """Lists the replica nodes of a master node.

        Deprecated since Redis 5.0.0, replaced by ``CLUSTER REPLICAS``.

        Syntax::

            CLUSTER SLAVES node-id

        Reply: a list of replica nodes replicating from the specified master node provided in
        the same format used by CLUSTER NODES

        Time complexity: O(N) where N is the number of replicas.

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'SLAVES', ...)``.

        https://redis.io/docs/latest/commands/cluster-slaves/
        """

    def cluster_slot_stats(self, *args: Any) -> Any:
        """Return an array of slot usage statistics for slots assigned to the current node.

        Syntax::

            CLUSTER SLOT-STATS <SLOTSRANGE start-slot end-slot | ORDERBY metric [LIMIT limit]
                [ASC | DESC]>

        Reply: Array of nested arrays, where the inner array element represents a slot and its
        respective usage statistics.

        Time complexity: O(N) where N is the total number of slots based on arguments.
        O(N*log(N)) with ORDERBY subcommand.

        Available since Redis 8.2.0. Compiles to ``redis.call('CLUSTER', 'SLOT-STATS', ...)``.

        https://redis.io/docs/latest/commands/cluster-slot-stats/
        """

    def cluster_slots(self) -> Any:
        """Returns the mapping of cluster slots to nodes.

        Deprecated since Redis 7.0.0, replaced by ``CLUSTER SHARDS``.

        Syntax::

            CLUSTER SLOTS

        Reply: nested list of slot ranges with networking information

        Time complexity: O(N) where N is the total number of Cluster nodes

        Available since Redis 3.0.0. Compiles to ``redis.call('CLUSTER', 'SLOTS')``.

        https://redis.io/docs/latest/commands/cluster-slots/
        """

    def cluster_syncslots(self, *args: Any) -> Any:
        """Internal command for atomic slot migration protocol between cluster nodes.

        Syntax::

            CLUSTER SYNCSLOTS <SYNC task-id start-slot end-slot [start-slot end-slot ...] |
                RDBCHANNEL task-id | SNAPSHOT-EOF | STREAM-EOF | ACK state offset | FAIL error |
                CONF option [option ...] value [value ...]>

        Reply, one of:

        - Reply to CLUSTER SYNCSLOTS SYNC, returns special RDB channel sync response.
        - Reply to CLUSTER SYNCSLOTS CONF and other subcommands.

        Time complexity: O(1)

        Available since Redis 8.4.0. Compiles to ``redis.call('CLUSTER', 'SYNCSLOTS', ...)``.

        https://redis.io/docs/latest/commands/cluster-syncslots/
        """

    def command_count(self) -> Any:
        """Returns a count of commands.

        Syntax::

            COMMAND COUNT

        Reply: Number of total commands in this Redis server.

        Time complexity: O(1)

        Available since Redis 2.8.13. Compiles to ``redis.call('COMMAND', 'COUNT')``.

        https://redis.io/docs/latest/commands/command-count/
        """

    def command_docs(self, *args: Any) -> Any:
        """Returns documentary information about one, multiple or all commands.

        Syntax::

            COMMAND DOCS [command-name [command-name ...]]

        Reply: A map where each key is a command name, and each value is the documentary
        information

        Time complexity: O(N) where N is the number of commands to look up

        Available since Redis 7.0.0. Compiles to ``redis.call('COMMAND', 'DOCS', ...)``.

        https://redis.io/docs/latest/commands/command-docs/
        """

    def command_getkeys(self, command: Any, /, *args: Any) -> Any:
        """Extracts the key names from an arbitrary command.

        Syntax::

            COMMAND GETKEYS command [arg [arg ...]]

        Reply: List of keys from the given Redis command.

        Time complexity: O(N) where N is the number of arguments to the command

        Available since Redis 2.8.13. Compiles to ``redis.call('COMMAND', 'GETKEYS', ...)``.

        https://redis.io/docs/latest/commands/command-getkeys/
        """

    def command_getkeysandflags(self, command: Any, /, *args: Any) -> Any:
        """Extracts the key names and access flags for an arbitrary command.

        Syntax::

            COMMAND GETKEYSANDFLAGS command [arg [arg ...]]

        Reply: List of keys from the given Redis command and their usage flags.

        Time complexity: O(N) where N is the number of arguments to the command

        Available since Redis 7.0.0. Compiles to ``redis.call('COMMAND', 'GETKEYSANDFLAGS',
        ...)``.

        https://redis.io/docs/latest/commands/command-getkeysandflags/
        """

    def command_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            COMMAND HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('COMMAND', 'HELP')``.

        https://redis.io/docs/latest/commands/command-help/
        """

    def command_info(self, *args: Any) -> Any:
        """Returns information about one, multiple or all commands.

        Syntax::

            COMMAND INFO [command-name [command-name ...]]

        Time complexity: O(N) where N is the number of commands to look up

        Available since Redis 2.8.13. Compiles to ``redis.call('COMMAND', 'INFO', ...)``.

        https://redis.io/docs/latest/commands/command-info/
        """

    def command_list(self, *args: Any) -> Any:
        """Returns a list of command names.

        Syntax::

            COMMAND LIST [FILTERBY <MODULE module-name | ACLCAT category | PATTERN pattern>]

        Time complexity: O(N) where N is the total number of Redis commands

        Available since Redis 7.0.0. Compiles to ``redis.call('COMMAND', 'LIST', ...)``.

        https://redis.io/docs/latest/commands/command-list/
        """

    def config_get(self, parameter: Any, /, *args: Any) -> Any:
        """Returns the effective values of configuration parameters.

        Syntax::

            CONFIG GET parameter [parameter ...]

        Time complexity: O(N) when N is the number of configuration parameters provided

        Available since Redis 2.0.0. Compiles to ``redis.call('CONFIG', 'GET', ...)``.

        https://redis.io/docs/latest/commands/config-get/
        """

    def config_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            CONFIG HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('CONFIG', 'HELP')``.

        https://redis.io/docs/latest/commands/config-help/
        """

    def config_resetstat(self) -> Any:
        """Resets the server's statistics.

        Syntax::

            CONFIG RESETSTAT

        Time complexity: O(1)

        Available since Redis 2.0.0. Compiles to ``redis.call('CONFIG', 'RESETSTAT')``.

        https://redis.io/docs/latest/commands/config-resetstat/
        """

    def config_rewrite(self) -> Any:
        """Persists the effective configuration to file.

        Syntax::

            CONFIG REWRITE

        Time complexity: O(1)

        Available since Redis 2.8.0. Compiles to ``redis.call('CONFIG', 'REWRITE')``.

        https://redis.io/docs/latest/commands/config-rewrite/
        """

    def config_set(self, *args: Any) -> Any:
        """Sets configuration parameters in-flight.

        Syntax::

            CONFIG SET parameter value [parameter value ...]

        Time complexity: O(N) when N is the number of configuration parameters provided

        Available since Redis 2.0.0. Compiles to ``redis.call('CONFIG', 'SET', ...)``.

        https://redis.io/docs/latest/commands/config-set/
        """

    def copy(self, source: Any, destination: Any, /, *args: Any) -> Any:
        """Copies the value of a key to a new key.

        Syntax::

            COPY source destination [DB destination-db] [REPLACE]

        Reply, one of:

        - source was copied
        - source was not copied

        Time complexity: O(N) worst case for collections, where N is the number of nested items.
        O(1) for string values.

        Available since Redis 6.2.0. Compiles to ``redis.call('COPY', ...)``.

        https://redis.io/docs/latest/commands/copy/
        """

    def dbsize(self) -> Any:
        """Returns the number of keys in the database.

        Syntax::

            DBSIZE

        Reply: The number of keys in the currently-selected database.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('DBSIZE')``.

        https://redis.io/docs/latest/commands/dbsize/
        """

    def decr(self, key: Any, /) -> Any:
        """Decrements the integer value of a key by one. Uses 0 as initial value if the key doesn't
        exist.

        Syntax::

            DECR key

        Reply: The value of the key after decrementing it.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('DECR', ...)``.

        https://redis.io/docs/latest/commands/decr/
        """

    def decrby(self, key: Any, decrement: Any, /) -> Any:
        """Decrements a number from the integer value of a key. Uses 0 as initial value if the key
        doesn't exist.

        Syntax::

            DECRBY key decrement

        Reply: The value of the key after decrementing it.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('DECRBY', ...)``.

        https://redis.io/docs/latest/commands/decrby/
        """

    def delete(self, key: Any, /, *args: Any) -> Any:
        """Deletes one or more keys.

        Syntax::

            DEL key [key ...]

        Reply: the number of keys that were removed

        Time complexity: O(N) where N is the number of keys that will be removed. When a key to
        remove holds a value other than a string, the individual complexity for this key is O(M)
        where M is the number of elements in the list, set, sorted set or hash. Removing a
        single key that holds a string value is O(1).

        Available since Redis 1.0.0. Compiles to ``redis.call('DEL', ...)``.

        https://redis.io/docs/latest/commands/del/
        """

    def delex(self, key: Any, /, *args: Any) -> Any:
        """Conditionally removes the specified key based on value or digest comparison.

        Syntax::

            DELEX key [IFEQ ifeq-value | IFNE ifne-value | IFDEQ ifdeq-digest | IFDNE
                ifdne-digest]

        Reply, one of:

        - The key exists but holds a non-string value
        - The key does not exist or the specified condition was not met.
        - The key was deleted.

        Time complexity: O(1) for IFEQ/IFNE, O(N) for IFDEQ/IFDNE where N is the length of the
        string value.

        Available since Redis 8.4.0. Compiles to ``redis.call('DELEX', ...)``.

        https://redis.io/docs/latest/commands/delex/
        """

    def digest(self, key: Any, /) -> Any:
        """Returns the XXH3 hash of a string value.

        Syntax::

            DIGEST key

        Reply, one of:

        - The XXH3 64-bit hash of the string value as a signed integer.
        - Key does not exist

        Time complexity: O(N) where N is the length of the string value.

        Available since Redis 8.4.0. Compiles to ``redis.call('DIGEST', ...)``.

        https://redis.io/docs/latest/commands/digest/
        """

    def discard(self) -> Any:
        """Discards a transaction.

        Syntax::

            DISCARD

        Time complexity: O(N), when N is the number of queued commands

        Available since Redis 2.0.0. Compiles to ``redis.call('DISCARD')``.

        https://redis.io/docs/latest/commands/discard/
        """

    def dump(self, key: Any, /) -> Any:
        """Returns a serialized representation of the value stored at a key.

        Syntax::

            DUMP key

        Reply, one of:

        - The serialized value.
        - Key does not exist.

        Time complexity: O(1) to access the key and additional O(N*M) to serialize it, where N
        is the number of Redis objects composing the value and M their average size. For small
        string values the time complexity is thus O(1)+O(1*M) where M is small, so simply O(1).

        Available since Redis 2.6.0. Compiles to ``redis.call('DUMP', ...)``.

        https://redis.io/docs/latest/commands/dump/
        """

    def echo(self, message: Any, /) -> Any:
        """Returns the given string.

        Syntax::

            ECHO message

        Reply: The given string

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('ECHO', ...)``.

        https://redis.io/docs/latest/commands/echo/
        """

    def eval(self, script: Any, numkeys: Any, /, *args: Any) -> Any:
        """Executes a server-side Lua script.

        Syntax::

            EVAL script numkeys [key [key ...]] [arg [arg ...]]

        Reply: Return value depends on the script that is executed

        Time complexity: Depends on the script that is executed.

        Available since Redis 2.6.0. Compiles to ``redis.call('EVAL', ...)``.

        https://redis.io/docs/latest/commands/eval/
        """

    def eval_ro(self, script: Any, numkeys: Any, /, *args: Any) -> Any:
        """Executes a read-only server-side Lua script.

        Syntax::

            EVAL_RO script numkeys [key [key ...]] [arg [arg ...]]

        Reply: Return value depends on the script that is executed

        Time complexity: Depends on the script that is executed.

        Available since Redis 7.0.0. Compiles to ``redis.call('EVAL_RO', ...)``.

        https://redis.io/docs/latest/commands/eval_ro/
        """

    def evalsha(self, sha1: Any, numkeys: Any, /, *args: Any) -> Any:
        """Executes a server-side Lua script by SHA1 digest.

        Syntax::

            EVALSHA sha1 numkeys [key [key ...]] [arg [arg ...]]

        Reply: Return value depends on the script that is executed

        Time complexity: Depends on the script that is executed.

        Available since Redis 2.6.0. Compiles to ``redis.call('EVALSHA', ...)``.

        https://redis.io/docs/latest/commands/evalsha/
        """

    def evalsha_ro(self, sha1: Any, numkeys: Any, /, *args: Any) -> Any:
        """Executes a read-only server-side Lua script by SHA1 digest.

        Syntax::

            EVALSHA_RO sha1 numkeys [key [key ...]] [arg [arg ...]]

        Reply: Return value depends on the script that is executed

        Time complexity: Depends on the script that is executed.

        Available since Redis 7.0.0. Compiles to ``redis.call('EVALSHA_RO', ...)``.

        https://redis.io/docs/latest/commands/evalsha_ro/
        """

    def exec(self) -> Any:
        """Executes all commands in a transaction.

        Syntax::

            EXEC

        Reply, one of:

        - Each element being the reply to each of the commands in the atomic transaction.
        - The transaction was aborted because a ``WATCH``ed key was touched

        Time complexity: Depends on commands in the transaction

        Available since Redis 1.2.0. Compiles to ``redis.call('EXEC')``.

        https://redis.io/docs/latest/commands/exec/
        """

    def exists(self, key: Any, /, *args: Any) -> Any:
        """Determines whether one or more keys exist.

        Syntax::

            EXISTS key [key ...]

        Reply: Number of keys that exist from those specified as arguments.

        Time complexity: O(N) where N is the number of keys to check.

        Available since Redis 1.0.0. Compiles to ``redis.call('EXISTS', ...)``.

        https://redis.io/docs/latest/commands/exists/
        """

    def expire(self, key: Any, seconds: Any, /, *args: Any) -> Any:
        """Sets the expiration time of a key in seconds.

        Syntax::

            EXPIRE key seconds [NX | XX | GT | LT]

        Reply, one of:

        - The timeout was not set. e.g. key doesn't exist, or operation skipped due to the
          provided arguments.
        - The timeout was set.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('EXPIRE', ...)``.

        https://redis.io/docs/latest/commands/expire/
        """

    def expireat(self, key: Any, unix_time_seconds: Any, /, *args: Any) -> Any:
        """Sets the expiration time of a key to a Unix timestamp.

        Syntax::

            EXPIREAT key unix-time-seconds [NX | XX | GT | LT]

        Reply, one of:

        - The timeout was set.
        - The timeout was not set. e.g. key doesn't exist, or operation skipped due to the
          provided arguments.

        Time complexity: O(1)

        Available since Redis 1.2.0. Compiles to ``redis.call('EXPIREAT', ...)``.

        https://redis.io/docs/latest/commands/expireat/
        """

    def expiretime(self, key: Any, /) -> Any:
        """Returns the expiration time of a key as a Unix timestamp.

        Syntax::

            EXPIRETIME key

        Reply, one of:

        - Expiration Unix timestamp in seconds.
        - The key exists but has no associated expiration time.
        - The key does not exist.

        Time complexity: O(1)

        Available since Redis 7.0.0. Compiles to ``redis.call('EXPIRETIME', ...)``.

        https://redis.io/docs/latest/commands/expiretime/
        """

    def failover(self, *args: Any) -> Any:
        """Starts a coordinated failover from a server to one of its replicas.

        Syntax::

            FAILOVER [TO host port [FORCE]] [ABORT] [TIMEOUT milliseconds]

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('FAILOVER', ...)``.

        https://redis.io/docs/latest/commands/failover/
        """

    def fcall(self, function: Any, numkeys: Any, /, *args: Any) -> Any:
        """Invokes a function.

        Syntax::

            FCALL function numkeys [key [key ...]] [arg [arg ...]]

        Reply: Return value depends on the function that is executed

        Time complexity: Depends on the function that is executed.

        Available since Redis 7.0.0. Compiles to ``redis.call('FCALL', ...)``.

        https://redis.io/docs/latest/commands/fcall/
        """

    def fcall_ro(self, function: Any, numkeys: Any, /, *args: Any) -> Any:
        """Invokes a read-only function.

        Syntax::

            FCALL_RO function numkeys [key [key ...]] [arg [arg ...]]

        Reply: Return value depends on the function that is executed

        Time complexity: Depends on the function that is executed.

        Available since Redis 7.0.0. Compiles to ``redis.call('FCALL_RO', ...)``.

        https://redis.io/docs/latest/commands/fcall_ro/
        """

    def flushall(self, *args: Any) -> Any:
        """Removes all keys from all databases.

        Syntax::

            FLUSHALL [ASYNC | SYNC]

        Time complexity: O(N) where N is the total number of keys in all databases

        Available since Redis 1.0.0. Compiles to ``redis.call('FLUSHALL', ...)``.

        https://redis.io/docs/latest/commands/flushall/
        """

    def flushdb(self, *args: Any) -> Any:
        """Remove all keys from the current database.

        Syntax::

            FLUSHDB [ASYNC | SYNC]

        Time complexity: O(N) where N is the number of keys in the selected database

        Available since Redis 1.0.0. Compiles to ``redis.call('FLUSHDB', ...)``.

        https://redis.io/docs/latest/commands/flushdb/
        """

    def function_delete(self, library_name: Any, /) -> Any:
        """Deletes a library and its functions.

        Syntax::

            FUNCTION DELETE library-name

        Time complexity: O(1)

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'DELETE', ...)``.

        https://redis.io/docs/latest/commands/function-delete/
        """

    def function_dump(self) -> Any:
        """Dumps all libraries into a serialized binary payload.

        Syntax::

            FUNCTION DUMP

        Reply: the serialized payload

        Time complexity: O(N) where N is the number of functions

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'DUMP')``.

        https://redis.io/docs/latest/commands/function-dump/
        """

    def function_flush(self, *args: Any) -> Any:
        """Deletes all libraries and functions.

        Syntax::

            FUNCTION FLUSH [ASYNC | SYNC]

        Time complexity: O(N) where N is the number of functions deleted

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'FLUSH', ...)``.

        https://redis.io/docs/latest/commands/function-flush/
        """

    def function_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            FUNCTION HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'HELP')``.

        https://redis.io/docs/latest/commands/function-help/
        """

    def function_kill(self) -> Any:
        """Terminates a function during execution.

        Syntax::

            FUNCTION KILL

        Time complexity: O(1)

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'KILL')``.

        https://redis.io/docs/latest/commands/function-kill/
        """

    def function_list(self, *args: Any) -> Any:
        """Returns information about all libraries.

        Syntax::

            FUNCTION LIST [LIBRARYNAME library-name-pattern] [WITHCODE]

        Time complexity: O(N) where N is the number of functions

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'LIST', ...)``.

        https://redis.io/docs/latest/commands/function-list/
        """

    def function_load(self, *args: Any) -> Any:
        """Creates a library.

        Syntax::

            FUNCTION LOAD [REPLACE] function-code

        Reply: The library name that was loaded

        Time complexity: O(1) (considering compilation time is redundant)

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'LOAD', ...)``.

        https://redis.io/docs/latest/commands/function-load/
        """

    def function_restore(self, serialized_value: Any, /, *args: Any) -> Any:
        """Restores all libraries from a payload.

        Syntax::

            FUNCTION RESTORE serialized-value [FLUSH | APPEND | REPLACE]

        Time complexity: O(N) where N is the number of functions on the payload

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'RESTORE', ...)``.

        https://redis.io/docs/latest/commands/function-restore/
        """

    def function_stats(self) -> Any:
        """Returns information about a function during execution.

        Syntax::

            FUNCTION STATS

        Time complexity: O(1)

        Available since Redis 7.0.0. Compiles to ``redis.call('FUNCTION', 'STATS')``.

        https://redis.io/docs/latest/commands/function-stats/
        """

    def geoadd(self, key: Any, /, *args: Any) -> Any:
        """Adds one or more members to a geospatial index. The key is created if it doesn't exist.

        Syntax::

            GEOADD key [NX | XX] [CH] longitude latitude member [longitude latitude member ...]

        Reply: When used without optional arguments, the number of elements added to the sorted
        set (excluding score updates). If the CH option is specified, the number of elements
        that were changed (added or updated).

        Time complexity: O(log(N)) for each item added, where N is the number of elements in the
        sorted set.

        Available since Redis 3.2.0. Compiles to ``redis.call('GEOADD', ...)``.

        https://redis.io/docs/latest/commands/geoadd/
        """

    def geodist(self, key: Any, member1: Any, member2: Any, /, *args: Any) -> Any:
        """Returns the distance between two members of a geospatial index.

        Syntax::

            GEODIST key member1 member2 [m | km | ft | mi]

        Reply, one of:

        - one or both of elements are missing
        - distance as a double (represented as a string) in the specified units

        Time complexity: O(1)

        Available since Redis 3.2.0. Compiles to ``redis.call('GEODIST', ...)``.

        https://redis.io/docs/latest/commands/geodist/
        """

    def geohash(self, key: Any, /, *args: Any) -> Any:
        """Returns members from a geospatial index as geohash strings.

        Syntax::

            GEOHASH key [member [member ...]]

        Reply: An array where each element is the Geohash corresponding to each member name
        passed as argument to the command.

        Time complexity: O(1) for each member requested.

        Available since Redis 3.2.0. Compiles to ``redis.call('GEOHASH', ...)``.

        https://redis.io/docs/latest/commands/geohash/
        """

    def geopos(self, key: Any, /, *args: Any) -> Any:
        """Returns the longitude and latitude of members from a geospatial index.

        Syntax::

            GEOPOS key [member [member ...]]

        Reply: An array where each element is a two elements array representing longitude and
        latitude (x,y) of each member name passed as argument to the command

        Time complexity: O(1) for each member requested.

        Available since Redis 3.2.0. Compiles to ``redis.call('GEOPOS', ...)``.

        https://redis.io/docs/latest/commands/geopos/
        """

    def georadius(self, key: Any, longitude: Any, latitude: Any, radius: Any, /, *args: Any) -> Any:
        """Queries a geospatial index for members within a distance from a coordinate, optionally
        stores the result.

        Deprecated since Redis 6.2.0, replaced by ``GEOSEARCH`` and ``GEOSEARCHSTORE`` with the
        ``BYRADIUS`` argument.

        Syntax::

            GEORADIUS key longitude latitude radius <m | km | ft | mi> [WITHCOORD] [WITHDIST]
                [WITHHASH] [COUNT count [ANY]] [ASC | DESC] [STORE key | STOREDIST key]

        Reply: Array of matched members information

        Time complexity: O(N+log(M)) where N is the number of elements inside the bounding box
        of the circular area delimited by center and radius and M is the number of items inside
        the index.

        Available since Redis 3.2.0. Compiles to ``redis.call('GEORADIUS', ...)``.

        https://redis.io/docs/latest/commands/georadius/
        """

    def georadius_ro(
        self, key: Any, longitude: Any, latitude: Any, radius: Any, /, *args: Any
    ) -> Any:
        """Returns members from a geospatial index that are within a distance from a coordinate.

        Deprecated since Redis 6.2.0, replaced by ``GEOSEARCH`` with the ``BYRADIUS`` argument.

        Syntax::

            GEORADIUS_RO key longitude latitude radius <m | km | ft | mi> [WITHCOORD] [WITHDIST]
                [WITHHASH] [COUNT count [ANY]] [ASC | DESC]

        Reply: Array of matched members information

        Time complexity: O(N+log(M)) where N is the number of elements inside the bounding box
        of the circular area delimited by center and radius and M is the number of items inside
        the index.

        Available since Redis 3.2.10. Compiles to ``redis.call('GEORADIUS_RO', ...)``.

        https://redis.io/docs/latest/commands/georadius_ro/
        """

    def georadiusbymember(self, key: Any, member: Any, radius: Any, /, *args: Any) -> Any:
        """Queries a geospatial index for members within a distance from a member, optionally
        stores the result.

        Deprecated since Redis 6.2.0, replaced by ``GEOSEARCH`` and ``GEOSEARCHSTORE`` with the
        ``BYRADIUS`` and ``FROMMEMBER`` arguments.

        Syntax::

            GEORADIUSBYMEMBER key member radius <m | km | ft | mi> [WITHCOORD] [WITHDIST]
                [WITHHASH] [COUNT count [ANY]] [ASC | DESC] [STORE key | STOREDIST key]

        Reply: Array of matched members information

        Time complexity: O(N+log(M)) where N is the number of elements inside the bounding box
        of the circular area delimited by center and radius and M is the number of items inside
        the index.

        Available since Redis 3.2.0. Compiles to ``redis.call('GEORADIUSBYMEMBER', ...)``.

        https://redis.io/docs/latest/commands/georadiusbymember/
        """

    def georadiusbymember_ro(self, key: Any, member: Any, radius: Any, /, *args: Any) -> Any:
        """Returns members from a geospatial index that are within a distance from a member.

        Deprecated since Redis 6.2.0, replaced by ``GEOSEARCH`` with the ``BYRADIUS`` and
        ``FROMMEMBER`` arguments.

        Syntax::

            GEORADIUSBYMEMBER_RO key member radius <m | km | ft | mi> [WITHCOORD] [WITHDIST]
                [WITHHASH] [COUNT count [ANY]] [ASC | DESC]

        Reply: Array of matched members information

        Time complexity: O(N+log(M)) where N is the number of elements inside the bounding box
        of the circular area delimited by center and radius and M is the number of items inside
        the index.

        Available since Redis 3.2.10. Compiles to ``redis.call('GEORADIUSBYMEMBER_RO', ...)``.

        https://redis.io/docs/latest/commands/georadiusbymember_ro/
        """

    def geosearch(self, key: Any, /, *args: Any) -> Any:
        """Queries a geospatial index for members inside an area of a box or a circle.

        Syntax::

            GEOSEARCH key <FROMMEMBER member | FROMLONLAT longitude latitude> <BYRADIUS radius
                <m | km | ft | mi> | BYBOX width height <m | km | ft | mi>> [ASC | DESC] [COUNT
                count [ANY]] [WITHCOORD] [WITHDIST] [WITHHASH]

        Reply: Array of matched members information

        Time complexity: O(N+log(M)) where N is the number of elements in the grid-aligned
        bounding box area around the shape provided as the filter and M is the number of items
        inside the shape

        Available since Redis 6.2.0. Compiles to ``redis.call('GEOSEARCH', ...)``.

        https://redis.io/docs/latest/commands/geosearch/
        """

    def geosearchstore(self, destination: Any, source: Any, /, *args: Any) -> Any:
        """Queries a geospatial index for members inside an area of a box or a circle, optionally
        stores the result.

        Syntax::

            GEOSEARCHSTORE destination source <FROMMEMBER member | FROMLONLAT longitude
                latitude> <BYRADIUS radius <m | km | ft | mi> | BYBOX width height <m | km | ft
                | mi>> [ASC | DESC] [COUNT count [ANY]] [STOREDIST]

        Reply: the number of elements in the resulting set

        Time complexity: O(N+log(M)) where N is the number of elements in the grid-aligned
        bounding box area around the shape provided as the filter and M is the number of items
        inside the shape

        Available since Redis 6.2.0. Compiles to ``redis.call('GEOSEARCHSTORE', ...)``.

        https://redis.io/docs/latest/commands/geosearchstore/
        """

    def get(self, key: Any, /) -> Any:
        """Returns the string value of a key.

        Syntax::

            GET key

        Reply, one of:

        - The value of the key.
        - Key does not exist.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('GET', ...)``.

        https://redis.io/docs/latest/commands/get/
        """

    def getbit(self, key: Any, offset: Any, /) -> Any:
        """Returns a bit value by offset.

        Syntax::

            GETBIT key offset

        Reply: The bit value stored at offset.

        Time complexity: O(1)

        Available since Redis 2.2.0. Compiles to ``redis.call('GETBIT', ...)``.

        https://redis.io/docs/latest/commands/getbit/
        """

    def getdel(self, key: Any, /) -> Any:
        """Returns the string value of a key after deleting the key.

        Syntax::

            GETDEL key

        Reply, one of:

        - The value of the key.
        - The key does not exist.

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('GETDEL', ...)``.

        https://redis.io/docs/latest/commands/getdel/
        """

    def getex(self, key: Any, /, *args: Any) -> Any:
        """Returns the string value of a key after setting its expiration time.

        Syntax::

            GETEX key [EX seconds | PX milliseconds | EXAT unix-time-seconds | PXAT
                unix-time-milliseconds | PERSIST]

        Reply, one of:

        - The value of the key.
        - Key does not exist.

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('GETEX', ...)``.

        https://redis.io/docs/latest/commands/getex/
        """

    def getrange(self, key: Any, start: Any, end: Any, /) -> Any:
        """Returns a substring of the string stored at a key.

        Syntax::

            GETRANGE key start end

        Reply: The substring of the string value stored at key, determined by the offsets start
        and end (both are inclusive).

        Time complexity: O(N) where N is the length of the returned string. The complexity is
        ultimately determined by the returned length, but because creating a substring from an
        existing string is very cheap, it can be considered O(1) for small strings.

        Available since Redis 2.4.0. Compiles to ``redis.call('GETRANGE', ...)``.

        https://redis.io/docs/latest/commands/getrange/
        """

    def getset(self, key: Any, value: Any, /) -> Any:
        """Returns the previous string value of a key after setting it to a new value.

        Deprecated since Redis 6.2.0, replaced by ``SET`` with the ``GET`` argument.

        Syntax::

            GETSET key value

        Reply, one of:

        - The old value stored at the key.
        - The key does not exist.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('GETSET', ...)``.

        https://redis.io/docs/latest/commands/getset/
        """

    def hdel(self, key: Any, field: Any, /, *args: Any) -> Any:
        """Deletes one or more fields and their values from a hash. Deletes the hash if no fields
        remain.

        Syntax::

            HDEL key field [field ...]

        Reply: The number of fields that were removed from the hash.

        Time complexity: O(N) where N is the number of fields to be removed.

        Available since Redis 2.0.0. Compiles to ``redis.call('HDEL', ...)``.

        https://redis.io/docs/latest/commands/hdel/
        """

    def hello(self, *args: Any) -> Any:
        """Handshakes with the Redis server.

        Syntax::

            HELLO [protover [AUTH username password] [SETNAME clientname]]

        Time complexity: O(1)

        Available since Redis 6.0.0. Compiles to ``redis.call('HELLO', ...)``.

        https://redis.io/docs/latest/commands/hello/
        """

    def hexists(self, key: Any, field: Any, /) -> Any:
        """Determines whether a field exists in a hash.

        Syntax::

            HEXISTS key field

        Reply, one of:

        - The hash does not contain the field, or key does not exist.
        - The hash contains the field.

        Time complexity: O(1)

        Available since Redis 2.0.0. Compiles to ``redis.call('HEXISTS', ...)``.

        https://redis.io/docs/latest/commands/hexists/
        """

    def hexpire(self, key: Any, seconds: Any, /, *args: Any) -> Any:
        """Set expiry for hash field using relative time to expire (seconds)

        Syntax::

            HEXPIRE key seconds [NX | XX | GT | LT] FIELDS numfields field [field ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HEXPIRE', ...)``.

        https://redis.io/docs/latest/commands/hexpire/
        """

    def hexpireat(self, key: Any, unix_time_seconds: Any, /, *args: Any) -> Any:
        """Set expiry for hash field using an absolute Unix timestamp (seconds)

        Syntax::

            HEXPIREAT key unix-time-seconds [NX | XX | GT | LT] FIELDS numfields field [field
                ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HEXPIREAT', ...)``.

        https://redis.io/docs/latest/commands/hexpireat/
        """

    def hexpiretime(self, key: Any, /, *args: Any) -> Any:
        """Returns the expiration time of a hash field as a Unix timestamp, in seconds.

        Syntax::

            HEXPIRETIME key FIELDS numfields field [field ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HEXPIRETIME', ...)``.

        https://redis.io/docs/latest/commands/hexpiretime/
        """

    def hget(self, key: Any, field: Any, /) -> Any:
        """Returns the value of a field in a hash.

        Syntax::

            HGET key field

        Reply, one of:

        - The value associated with the field.
        - If the field is not present in the hash or key does not exist.

        Time complexity: O(1)

        Available since Redis 2.0.0. Compiles to ``redis.call('HGET', ...)``.

        https://redis.io/docs/latest/commands/hget/
        """

    def hgetall(self, key: Any, /) -> Any:
        """Returns all fields and values in a hash.

        Syntax::

            HGETALL key

        Reply: Map of fields and their values stored in the hash, or an empty list when key does
        not exist. In RESP2 this is returned as a flat array.

        Time complexity: O(N) where N is the size of the hash.

        Available since Redis 2.0.0. Compiles to ``redis.call('HGETALL', ...)``.

        https://redis.io/docs/latest/commands/hgetall/
        """

    def hgetdel(self, key: Any, /, *args: Any) -> Any:
        """Returns the value of a field and deletes it from the hash.

        Syntax::

            HGETDEL key FIELDS numfields field [field ...]

        Reply: List of values associated with the given fields, in the same order as they are
        requested.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 8.0.0. Compiles to ``redis.call('HGETDEL', ...)``.

        https://redis.io/docs/latest/commands/hgetdel/
        """

    def hgetex(self, key: Any, /, *args: Any) -> Any:
        """Get the value of one or more fields of a given hash key, and optionally set their
        expiration.

        Syntax::

            HGETEX key [EX seconds | PX milliseconds | EXAT unix-time-seconds | PXAT
                unix-time-milliseconds | PERSIST] FIELDS numfields field [field ...]

        Reply: List of values associated with the given fields, in the same order as they are
        requested.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 8.0.0. Compiles to ``redis.call('HGETEX', ...)``.

        https://redis.io/docs/latest/commands/hgetex/
        """

    def himport_discard(self, fieldset_name: Any, /) -> Any:
        """Removes a single session-local fieldset by name.

        Syntax::

            HIMPORT DISCARD fieldset-name

        Reply: 1 if the fieldset was removed, 0 if it did not exist.

        Time complexity: O(N) where N is the number of session-local fieldsets.

        Available since Redis 8.10.0. Compiles to ``redis.call('HIMPORT', 'DISCARD', ...)``.

        https://redis.io/docs/latest/commands/himport-discard/
        """

    def himport_discardall(self) -> Any:
        """Removes all session-local fieldsets for the connection.

        Syntax::

            HIMPORT DISCARDALL

        Reply: Number of fieldsets removed.

        Time complexity: O(N) where N is the number of session-local fieldsets.

        Available since Redis 8.10.0. Compiles to ``redis.call('HIMPORT', 'DISCARDALL')``.

        https://redis.io/docs/latest/commands/himport-discardall/
        """

    def himport_prepare(self, fieldset_name: Any, field: Any, /, *args: Any) -> Any:
        """Defines a session-local fieldset that maps a name to a sorted set of field names.

        Syntax::

            HIMPORT PREPARE fieldset-name field [field ...]

        Time complexity: O(N*log(N)) where N is the number of fields.

        Available since Redis 8.10.0. Compiles to ``redis.call('HIMPORT', 'PREPARE', ...)``.

        https://redis.io/docs/latest/commands/himport-prepare/
        """

    def himport_set(self, key: Any, fieldset_name: Any, value: Any, /, *args: Any) -> Any:
        """Creates a fieldset-based hash from values supplied in the order matching a previously
        prepared fieldset.

        Syntax::

            HIMPORT SET key fieldset-name value [value ...]

        Time complexity: O(N) where N is the number of fields in the fieldset.

        Available since Redis 8.10.0. Compiles to ``redis.call('HIMPORT', 'SET', ...)``.

        https://redis.io/docs/latest/commands/himport-set/
        """

    def hincrby(self, key: Any, field: Any, increment: Any, /) -> Any:
        """Increments the integer value of a field in a hash by a number. Uses 0 as initial value
        if the field doesn't exist.

        Syntax::

            HINCRBY key field increment

        Reply: The value of the field after the increment operation.

        Time complexity: O(1)

        Available since Redis 2.0.0. Compiles to ``redis.call('HINCRBY', ...)``.

        https://redis.io/docs/latest/commands/hincrby/
        """

    def hincrbyfloat(self, key: Any, field: Any, increment: Any, /) -> Any:
        """Increments the floating point value of a field by a number. Uses 0 as initial value if
        the field doesn't exist.

        Syntax::

            HINCRBYFLOAT key field increment

        Reply: The value of the field after the increment operation.

        Time complexity: O(1)

        Available since Redis 2.6.0. Compiles to ``redis.call('HINCRBYFLOAT', ...)``.

        https://redis.io/docs/latest/commands/hincrbyfloat/
        """

    def hkeys(self, key: Any, /) -> Any:
        """Returns all fields in a hash.

        Syntax::

            HKEYS key

        Reply: List of fields in the hash, or an empty list when the key does not exist.

        Time complexity: O(N) where N is the size of the hash.

        Available since Redis 2.0.0. Compiles to ``redis.call('HKEYS', ...)``.

        https://redis.io/docs/latest/commands/hkeys/
        """

    def hlen(self, key: Any, /) -> Any:
        """Returns the number of fields in a hash.

        Syntax::

            HLEN key

        Reply: Number of the fields in the hash, or 0 when the key does not exist.

        Time complexity: O(1)

        Available since Redis 2.0.0. Compiles to ``redis.call('HLEN', ...)``.

        https://redis.io/docs/latest/commands/hlen/
        """

    def hmget(self, key: Any, field: Any, /, *args: Any) -> Any:
        """Returns the values of all fields in a hash.

        Syntax::

            HMGET key field [field ...]

        Reply: List of values associated with the given fields, in the same order as they are
        requested.

        Time complexity: O(N) where N is the number of fields being requested.

        Available since Redis 2.0.0. Compiles to ``redis.call('HMGET', ...)``.

        https://redis.io/docs/latest/commands/hmget/
        """

    def hmset(self, key: Any, /, *args: Any) -> Any:
        """Sets the values of multiple fields.

        Deprecated since Redis 4.0.0, replaced by ``HSET`` with multiple field-value pairs.

        Syntax::

            HMSET key field value [field value ...]

        Time complexity: O(N) where N is the number of fields being set.

        Available since Redis 2.0.0. Compiles to ``redis.call('HMSET', ...)``.

        https://redis.io/docs/latest/commands/hmset/
        """

    def hotkeys_get(self) -> Any:
        """Returns lists of top K hotkeys depending on metrics chosen in HOTKEYS START command.

        Syntax::

            HOTKEYS GET

        Reply, one of:

        - Array of maps with various metrics (tracking-active, sample-ratio, selected-slots,
          time/network statistics), collection info (collection-start-time-unix-ms, collection-
          duration-ms, total-cpu-time-user-ms, total-cpu-time-sys-ms, total-net-bytes), and the
          requested lists of Top-K hotkeys (available metrics: by-cpu-time-us, by-net-bytes)
          where at most K hotkeys are returned.
        - If no tracking is started

        Time complexity: O(K) where K is the number of hotkeys returned.

        Available since Redis 8.6.0. Compiles to ``redis.call('HOTKEYS', 'GET')``.

        https://redis.io/docs/latest/commands/hotkeys-get/
        """

    def hotkeys_help(self) -> Any:
        """Return helpful text about HOTKEYS command parameters.

        Syntax::

            HOTKEYS HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 8.6.1. Compiles to ``redis.call('HOTKEYS', 'HELP')``.

        https://redis.io/docs/latest/commands/hotkeys-help/
        """

    def hotkeys_reset(self) -> Any:
        """Release the resources used for hotkey tracking.

        Syntax::

            HOTKEYS RESET

        Time complexity: O(1)

        Available since Redis 8.6.0. Compiles to ``redis.call('HOTKEYS', 'RESET')``.

        https://redis.io/docs/latest/commands/hotkeys-reset/
        """

    def hotkeys_start(self, *args: Any) -> Any:
        """Starts hotkeys tracking.

        Syntax::

            HOTKEYS START METRICS count [CPU] [NET] [COUNT k] [DURATION seconds] [SAMPLE ratio]
                [SLOTS count slot [slot ...]]

        Time complexity: O(1)

        Available since Redis 8.6.0. Compiles to ``redis.call('HOTKEYS', 'START', ...)``.

        https://redis.io/docs/latest/commands/hotkeys-start/
        """

    def hotkeys_stop(self) -> Any:
        """Stops hotkeys tracking.

        Syntax::

            HOTKEYS STOP

        Time complexity: O(1)

        Available since Redis 8.6.0. Compiles to ``redis.call('HOTKEYS', 'STOP')``.

        https://redis.io/docs/latest/commands/hotkeys-stop/
        """

    def hpersist(self, key: Any, /, *args: Any) -> Any:
        """Removes the expiration time for each specified field

        Syntax::

            HPERSIST key FIELDS numfields field [field ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HPERSIST', ...)``.

        https://redis.io/docs/latest/commands/hpersist/
        """

    def hpexpire(self, key: Any, milliseconds: Any, /, *args: Any) -> Any:
        """Set expiry for hash field using relative time to expire (milliseconds)

        Syntax::

            HPEXPIRE key milliseconds [NX | XX | GT | LT] FIELDS numfields field [field ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HPEXPIRE', ...)``.

        https://redis.io/docs/latest/commands/hpexpire/
        """

    def hpexpireat(self, key: Any, unix_time_milliseconds: Any, /, *args: Any) -> Any:
        """Set expiry for hash field using an absolute Unix timestamp (milliseconds)

        Syntax::

            HPEXPIREAT key unix-time-milliseconds [NX | XX | GT | LT] FIELDS numfields field
                [field ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HPEXPIREAT', ...)``.

        https://redis.io/docs/latest/commands/hpexpireat/
        """

    def hpexpiretime(self, key: Any, /, *args: Any) -> Any:
        """Returns the expiration time of a hash field as a Unix timestamp, in msec.

        Syntax::

            HPEXPIRETIME key FIELDS numfields field [field ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HPEXPIRETIME', ...)``.

        https://redis.io/docs/latest/commands/hpexpiretime/
        """

    def hpttl(self, key: Any, /, *args: Any) -> Any:
        """Returns the TTL in milliseconds of a hash field.

        Syntax::

            HPTTL key FIELDS numfields field [field ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HPTTL', ...)``.

        https://redis.io/docs/latest/commands/hpttl/
        """

    def hrandfield(self, key: Any, /, *args: Any) -> Any:
        """Returns one or more random fields from a hash.

        Syntax::

            HRANDFIELD key [count [WITHVALUES]]

        Reply, one of:

        - Key doesn't exist
        - A single random field. Returned in case ``COUNT`` was not used.
        - A list of fields. Returned in case ``COUNT`` was used.
        - Fields and their values. Returned in case ``COUNT`` and ``WITHVALUES`` were used. In
          RESP2 this is returned as a flat array.

        Time complexity: O(N) where N is the number of fields returned

        Available since Redis 6.2.0. Compiles to ``redis.call('HRANDFIELD', ...)``.

        https://redis.io/docs/latest/commands/hrandfield/
        """

    def hscan(self, key: Any, cursor: Any, /, *args: Any) -> Any:
        """Iterates over fields and values of a hash.

        Syntax::

            HSCAN key cursor [MATCH pattern] [COUNT count] [NOVALUES]

        Reply: cursor and scan response in array form

        Time complexity: O(1) for every call. O(N) for a complete iteration, including enough
        command calls for the cursor to return back to 0. N is the number of elements inside the
        collection.

        Available since Redis 2.8.0. Compiles to ``redis.call('HSCAN', ...)``.

        https://redis.io/docs/latest/commands/hscan/
        """

    def hset(self, key: Any, /, *args: Any) -> Any:
        """Creates or modifies the value of a field in a hash.

        Syntax::

            HSET key field value [field value ...]

        Reply: The number of fields that were added

        Time complexity: O(1) for each field/value pair added, so O(N) to add N field/value
        pairs when the command is called with multiple field/value pairs.

        Available since Redis 2.0.0. Compiles to ``redis.call('HSET', ...)``.

        https://redis.io/docs/latest/commands/hset/
        """

    def hsetex(self, key: Any, /, *args: Any) -> Any:
        """Set the value of one or more fields of a given hash key, and optionally set their
        expiration.

        Syntax::

            HSETEX key [FNX | FXX] [EX seconds | PX milliseconds | EXAT unix-time-seconds | PXAT
                unix-time-milliseconds | KEEPTTL] FIELDS numfields field value [field value ...]

        Reply, one of:

        - No field was set (due to FXX or FNX flags).
        - All the fields were set.

        Time complexity: O(N) where N is the number of fields being set.

        Available since Redis 8.0.0. Compiles to ``redis.call('HSETEX', ...)``.

        https://redis.io/docs/latest/commands/hsetex/
        """

    def hsetnx(self, key: Any, field: Any, value: Any, /) -> Any:
        """Sets the value of a field in a hash only when the field doesn't exist.

        Syntax::

            HSETNX key field value

        Reply, one of:

        - The field is a new field in the hash and value was set.
        - The field already exists in the hash and no operation was performed.

        Time complexity: O(1)

        Available since Redis 2.0.0. Compiles to ``redis.call('HSETNX', ...)``.

        https://redis.io/docs/latest/commands/hsetnx/
        """

    def hstrlen(self, key: Any, field: Any, /) -> Any:
        """Returns the length of the value of a field.

        Syntax::

            HSTRLEN key field

        Reply: String length of the value associated with the field, or zero when the field is
        not present in the hash or key does not exist at all.

        Time complexity: O(1)

        Available since Redis 3.2.0. Compiles to ``redis.call('HSTRLEN', ...)``.

        https://redis.io/docs/latest/commands/hstrlen/
        """

    def httl(self, key: Any, /, *args: Any) -> Any:
        """Returns the TTL in seconds of a hash field.

        Syntax::

            HTTL key FIELDS numfields field [field ...]

        Reply: Array of results. Returns empty array if the key does not exist.

        Time complexity: O(N) where N is the number of specified fields

        Available since Redis 7.4.0. Compiles to ``redis.call('HTTL', ...)``.

        https://redis.io/docs/latest/commands/httl/
        """

    def hvals(self, key: Any, /) -> Any:
        """Returns all values in a hash.

        Syntax::

            HVALS key

        Reply: List of values in the hash, or an empty list when the key does not exist.

        Time complexity: O(N) where N is the size of the hash.

        Available since Redis 2.0.0. Compiles to ``redis.call('HVALS', ...)``.

        https://redis.io/docs/latest/commands/hvals/
        """

    def incr(self, key: Any, /) -> Any:
        """Increments the integer value of a key by one. Uses 0 as initial value if the key doesn't
        exist.

        Syntax::

            INCR key

        Reply: The value of key after the increment

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('INCR', ...)``.

        https://redis.io/docs/latest/commands/incr/
        """

    def incrby(self, key: Any, increment: Any, /) -> Any:
        """Increments the integer value of a key by a number. Uses 0 as initial value if the key
        doesn't exist.

        Syntax::

            INCRBY key increment

        Reply: The value of the key after incrementing it.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('INCRBY', ...)``.

        https://redis.io/docs/latest/commands/incrby/
        """

    def incrbyfloat(self, key: Any, increment: Any, /) -> Any:
        """Increment the floating point value of a key by a number. Uses 0 as initial value if the
        key doesn't exist.

        Syntax::

            INCRBYFLOAT key increment

        Reply: The value of the key after incrementing it.

        Time complexity: O(1)

        Available since Redis 2.6.0. Compiles to ``redis.call('INCRBYFLOAT', ...)``.

        https://redis.io/docs/latest/commands/incrbyfloat/
        """

    def increx(self, key: Any, /, *args: Any) -> Any:
        """Increments the numeric value of a key by a number and sets its expiration time. Uses 0
        as initial value if the key doesn't exist.

        Syntax::

            INCREX key [BYFLOAT float | BYINT integer] [SATURATE] [LBOUND lowerbound] [UBOUND
                upperbound] [EX seconds | PX milliseconds | EXAT unix-time-seconds | PXAT
                unix-time-milliseconds | PERSIST] [ENX]

        Time complexity: O(1)

        Available since Redis 8.8.0. Compiles to ``redis.call('INCREX', ...)``.

        https://redis.io/docs/latest/commands/increx/
        """

    def info(self, *args: Any) -> Any:
        """Returns information and statistics about the server.

        Syntax::

            INFO [section [section ...]]

        Reply: A map of info fields, one field per line in the form of <field>:<value> where the
        value can be a comma separated map like <key>=<val>. Also contains section header lines
        starting with ``#`` and blank lines.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('INFO', ...)``.

        https://redis.io/docs/latest/commands/info/
        """

    def keys(self, pattern: Any, /) -> Any:
        """Returns all key names that match a pattern.

        Syntax::

            KEYS pattern

        Reply: list of keys matching pattern

        Time complexity: O(N) with N being the number of keys in the database, under the
        assumption that the key names in the database and the given pattern have limited length.

        Available since Redis 1.0.0. Compiles to ``redis.call('KEYS', ...)``.

        https://redis.io/docs/latest/commands/keys/
        """

    def lastsave(self) -> Any:
        """Returns the Unix timestamp of the last successful save to disk.

        Syntax::

            LASTSAVE

        Reply: UNIX TIME of the last DB save executed with success.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('LASTSAVE')``.

        https://redis.io/docs/latest/commands/lastsave/
        """

    def latency_doctor(self) -> Any:
        """Returns a human-readable latency analysis report.

        Syntax::

            LATENCY DOCTOR

        Reply: A human readable latency analysis report.

        Time complexity: O(1)

        Available since Redis 2.8.13. Compiles to ``redis.call('LATENCY', 'DOCTOR')``.

        https://redis.io/docs/latest/commands/latency-doctor/
        """

    def latency_graph(self, event: Any, /) -> Any:
        """Returns a latency graph for an event.

        Syntax::

            LATENCY GRAPH event

        Reply: Latency graph

        Time complexity: O(1)

        Available since Redis 2.8.13. Compiles to ``redis.call('LATENCY', 'GRAPH', ...)``.

        https://redis.io/docs/latest/commands/latency-graph/
        """

    def latency_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            LATENCY HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 2.8.13. Compiles to ``redis.call('LATENCY', 'HELP')``.

        https://redis.io/docs/latest/commands/latency-help/
        """

    def latency_histogram(self, *args: Any) -> Any:
        """Returns the cumulative distribution of latencies of a subset or all commands.

        Syntax::

            LATENCY HISTOGRAM [COMMAND [COMMAND ...]]

        Reply: A map where each key is a command name, and each value is a map with the total
        calls, and an inner map of the histogram time buckets.

        Time complexity: O(N) where N is the number of commands with latency information being
        retrieved.

        Available since Redis 7.0.0. Compiles to ``redis.call('LATENCY', 'HISTOGRAM', ...)``.

        https://redis.io/docs/latest/commands/latency-histogram/
        """

    def latency_history(self, event: Any, /) -> Any:
        """Returns timestamp-latency samples for an event.

        Syntax::

            LATENCY HISTORY event

        Reply: An array where each element is a two elements array representing the timestamp
        and the latency of the event.

        Time complexity: O(1)

        Available since Redis 2.8.13. Compiles to ``redis.call('LATENCY', 'HISTORY', ...)``.

        https://redis.io/docs/latest/commands/latency-history/
        """

    def latency_latest(self) -> Any:
        """Returns the latest latency samples for all events.

        Syntax::

            LATENCY LATEST

        Reply: An array where each element is a four elements array representing the event's
        name, timestamp, latest and all-time latency measurements.

        Time complexity: O(1)

        Available since Redis 2.8.13. Compiles to ``redis.call('LATENCY', 'LATEST')``.

        https://redis.io/docs/latest/commands/latency-latest/
        """

    def latency_reset(self, *args: Any) -> Any:
        """Resets the latency data for one or more events.

        Syntax::

            LATENCY RESET [event [event ...]]

        Reply: Number of event time series that were reset.

        Time complexity: O(1)

        Available since Redis 2.8.13. Compiles to ``redis.call('LATENCY', 'RESET', ...)``.

        https://redis.io/docs/latest/commands/latency-reset/
        """

    def lcs(self, key1: Any, key2: Any, /, *args: Any) -> Any:
        """Finds the longest common substring.

        Syntax::

            LCS key1 key2 [LEN] [IDX] [MINMATCHLEN min-match-len] [WITHMATCHLEN]

        Reply, one of:

        - The longest common subsequence.
        - The length of the longest common subsequence when 'LEN' is given.
        - Array with the LCS length and all the ranges in both the strings when 'IDX' is given.
          In RESP2 this is returned as a flat array

        Time complexity: O(N*M) where N and M are the lengths of s1 and s2, respectively

        Available since Redis 7.0.0. Compiles to ``redis.call('LCS', ...)``.

        https://redis.io/docs/latest/commands/lcs/
        """

    def lindex(self, key: Any, index: Any, /) -> Any:
        """Returns an element from a list by its index.

        Syntax::

            LINDEX key index

        Reply, one of:

        - Index is out of range
        - The requested element

        Time complexity: O(N) where N is the number of elements to traverse to get to the
        element at index. This makes asking for the first or the last element of the list O(1).

        Available since Redis 1.0.0. Compiles to ``redis.call('LINDEX', ...)``.

        https://redis.io/docs/latest/commands/lindex/
        """

    def linsert(self, key: Any, /, *args: Any) -> Any:
        """Inserts an element before or after another element in a list.

        Syntax::

            LINSERT key <BEFORE | AFTER> pivot element

        Reply, one of:

        - List length after a successful insert operation.
        - in case key doesn't exist.
        - when the pivot wasn't found.

        Time complexity: O(N) where N is the number of elements to traverse before seeing the
        value pivot. This means that inserting somewhere on the left end on the list (head) can
        be considered O(1) and inserting somewhere on the right end (tail) is O(N).

        Available since Redis 2.2.0. Compiles to ``redis.call('LINSERT', ...)``.

        https://redis.io/docs/latest/commands/linsert/
        """

    def llen(self, key: Any, /) -> Any:
        """Returns the length of a list.

        Syntax::

            LLEN key

        Reply: List length.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('LLEN', ...)``.

        https://redis.io/docs/latest/commands/llen/
        """

    def lmove(self, source: Any, destination: Any, /, *args: Any) -> Any:
        """Returns an element after popping it from one list and pushing it to another. Deletes the
        list if the last element was moved.

        Syntax::

            LMOVE source destination <LEFT | RIGHT> <LEFT | RIGHT>

        Reply: The element being popped and pushed.

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('LMOVE', ...)``.

        https://redis.io/docs/latest/commands/lmove/
        """

    def lmovem(self, source: Any, destination: Any, /, *args: Any) -> Any:
        """Moves up to (or exactly) a number of elements from one list to another and returns them.
        Deletes the source list if it becomes empty.

        Syntax::

            LMOVEM source destination <LEFT | RIGHT> <LEFT | RIGHT> [<COUNT count | EXACTLY
                exactly> <OBO | BULK>]

        Reply, one of:

        - Not enough elements to satisfy EXACTLY; nothing moved.
        - The moved elements, in destination order.

        Time complexity: O(N) where N is the number of elements moved.

        Available since Redis 8.10.0. Compiles to ``redis.call('LMOVEM', ...)``.

        https://redis.io/docs/latest/commands/lmovem/
        """

    def lmpop(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns multiple elements from a list after removing them. Deletes the list if the last
        element was popped.

        Syntax::

            LMPOP numkeys key [key ...] <LEFT | RIGHT> [COUNT count]

        Reply, one of:

        - If no element could be popped.
        - List key from which elements were popped.

        Time complexity: O(N+M) where N is the number of provided keys and M is the number of
        elements returned.

        Available since Redis 7.0.0. Compiles to ``redis.call('LMPOP', ...)``.

        https://redis.io/docs/latest/commands/lmpop/
        """

    def lolwut(self, *args: Any) -> Any:
        """Displays computer art and the Redis version

        Syntax::

            LOLWUT [VERSION version]

        Reply: String containing the generative computer art, and a text with the Redis version.

        Available since Redis 5.0.0. Compiles to ``redis.call('LOLWUT', ...)``.

        https://redis.io/docs/latest/commands/lolwut/
        """

    def lpop(self, key: Any, /, *args: Any) -> Any:
        """Returns the first elements in a list after removing it. Deletes the list if the last
        element was popped.

        Syntax::

            LPOP key [count]

        Reply, one of:

        - Key does not exist.
        - In case ``count`` argument was not given, the value of the first element.
        - In case ``count`` argument was given, a list of popped elements

        Time complexity: O(N) where N is the number of elements returned

        Available since Redis 1.0.0. Compiles to ``redis.call('LPOP', ...)``.

        https://redis.io/docs/latest/commands/lpop/
        """

    def lpos(self, key: Any, element: Any, /, *args: Any) -> Any:
        """Returns the index of matching elements in a list.

        Syntax::

            LPOS key element [RANK rank] [COUNT num-matches] [MAXLEN len]

        Reply, one of:

        - In case there is no matching element
        - An integer representing the matching element
        - If the COUNT option is given, an array of integers representing the matching elements
          (empty if there are no matches)

        Time complexity: O(N) where N is the number of elements in the list, for the average
        case. When searching for elements near the head or the tail of the list, or when the
        MAXLEN option is provided, the command may run in constant time.

        Available since Redis 6.0.6. Compiles to ``redis.call('LPOS', ...)``.

        https://redis.io/docs/latest/commands/lpos/
        """

    def lpush(self, key: Any, element: Any, /, *args: Any) -> Any:
        """Prepends one or more elements to a list. Creates the key if it doesn't exist.

        Syntax::

            LPUSH key element [element ...]

        Reply: Length of the list after the push operations.

        Time complexity: O(1) for each element added, so O(N) to add N elements when the command
        is called with multiple arguments.

        Available since Redis 1.0.0. Compiles to ``redis.call('LPUSH', ...)``.

        https://redis.io/docs/latest/commands/lpush/
        """

    def lpushx(self, key: Any, element: Any, /, *args: Any) -> Any:
        """Prepends one or more elements to a list only when the list exists.

        Syntax::

            LPUSHX key element [element ...]

        Reply: the length of the list after the push operation

        Time complexity: O(1) for each element added, so O(N) to add N elements when the command
        is called with multiple arguments.

        Available since Redis 2.2.0. Compiles to ``redis.call('LPUSHX', ...)``.

        https://redis.io/docs/latest/commands/lpushx/
        """

    def lrange(self, key: Any, start: Any, stop: Any, /) -> Any:
        """Returns a range of elements from a list.

        Syntax::

            LRANGE key start stop

        Reply: List of elements in the specified range

        Time complexity: O(S+N) where S is the distance of start offset from HEAD for small
        lists, from nearest end (HEAD or TAIL) for large lists; and N is the number of elements
        in the specified range.

        Available since Redis 1.0.0. Compiles to ``redis.call('LRANGE', ...)``.

        https://redis.io/docs/latest/commands/lrange/
        """

    def lrem(self, key: Any, count: Any, element: Any, /) -> Any:
        """Removes elements from a list. Deletes the list if the last element was removed.

        Syntax::

            LREM key count element

        Reply: The number of removed elements.

        Time complexity: O(N+M) where N is the length of the list and M is the number of
        elements removed.

        Available since Redis 1.0.0. Compiles to ``redis.call('LREM', ...)``.

        https://redis.io/docs/latest/commands/lrem/
        """

    def lset(self, key: Any, index: Any, element: Any, /) -> Any:
        """Sets the value of an element in a list by its index.

        Syntax::

            LSET key index element

        Time complexity: O(N) where N is the length of the list. Setting either the first or the
        last element of the list is O(1).

        Available since Redis 1.0.0. Compiles to ``redis.call('LSET', ...)``.

        https://redis.io/docs/latest/commands/lset/
        """

    def ltrim(self, key: Any, start: Any, stop: Any, /) -> Any:
        """Removes elements from both ends a list. Deletes the list if all elements were trimmed.

        Syntax::

            LTRIM key start stop

        Time complexity: O(N) where N is the number of elements to be removed by the operation.

        Available since Redis 1.0.0. Compiles to ``redis.call('LTRIM', ...)``.

        https://redis.io/docs/latest/commands/ltrim/
        """

    def memory_doctor(self) -> Any:
        """Outputs a memory problems report.

        Syntax::

            MEMORY DOCTOR

        Reply: memory problems report

        Time complexity: O(1)

        Available since Redis 4.0.0. Compiles to ``redis.call('MEMORY', 'DOCTOR')``.

        https://redis.io/docs/latest/commands/memory-doctor/
        """

    def memory_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            MEMORY HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 4.0.0. Compiles to ``redis.call('MEMORY', 'HELP')``.

        https://redis.io/docs/latest/commands/memory-help/
        """

    def memory_malloc_stats(self) -> Any:
        """Returns the allocator statistics.

        Syntax::

            MEMORY MALLOC-STATS

        Reply: The memory allocator's internal statistics report.

        Time complexity: Depends on how much memory is allocated, could be slow

        Available since Redis 4.0.0. Compiles to ``redis.call('MEMORY', 'MALLOC-STATS')``.

        https://redis.io/docs/latest/commands/memory-malloc-stats/
        """

    def memory_purge(self) -> Any:
        """Asks the allocator to release memory.

        Syntax::

            MEMORY PURGE

        Time complexity: Depends on how much memory is allocated, could be slow

        Available since Redis 4.0.0. Compiles to ``redis.call('MEMORY', 'PURGE')``.

        https://redis.io/docs/latest/commands/memory-purge/
        """

    def memory_stats(self) -> Any:
        """Returns details about memory usage.

        Syntax::

            MEMORY STATS

        Reply: memory usage details

        Time complexity: O(1)

        Available since Redis 4.0.0. Compiles to ``redis.call('MEMORY', 'STATS')``.

        https://redis.io/docs/latest/commands/memory-stats/
        """

    def memory_usage(self, key: Any, /, *args: Any) -> Any:
        """Estimates the memory usage of a key.

        Syntax::

            MEMORY USAGE key [SAMPLES count]

        Reply, one of:

        - Number of bytes that a key and its value require to be stored in RAM.
        - Key does not exist.

        Time complexity: O(N) where N is the number of samples.

        Available since Redis 4.0.0. Compiles to ``redis.call('MEMORY', 'USAGE', ...)``.

        https://redis.io/docs/latest/commands/memory-usage/
        """

    def mget(self, key: Any, /, *args: Any) -> Any:
        """Atomically returns the string values of one or more keys.

        Syntax::

            MGET key [key ...]

        Reply: List of values at the specified keys.

        Time complexity: O(N) where N is the number of keys to retrieve.

        Available since Redis 1.0.0. Compiles to ``redis.call('MGET', ...)``.

        https://redis.io/docs/latest/commands/mget/
        """

    def migrate(self, host: Any, port: Any, /, *args: Any) -> Any:
        """Atomically transfers a key from one Redis instance to another.

        Syntax::

            MIGRATE host port <key | ""> destination-db timeout [COPY] [REPLACE] [AUTH password
                | AUTH2 username password] [KEYS key [key ...]]

        Reply, one of:

        - Success.
        - No keys were found in the source instance.

        Time complexity: This command actually executes a DUMP+DEL in the source instance, and a
        RESTORE in the target instance. See the pages of these commands for time complexity.
        Also an O(N) data transfer between the two instances is performed.

        Available since Redis 2.6.0. Compiles to ``redis.call('MIGRATE', ...)``.

        https://redis.io/docs/latest/commands/migrate/
        """

    def module_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            MODULE HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('MODULE', 'HELP')``.

        https://redis.io/docs/latest/commands/module-help/
        """

    def module_list(self) -> Any:
        """Returns all loaded modules.

        Syntax::

            MODULE LIST

        Reply: Returns information about the modules loaded to the server.

        Time complexity: O(N) where N is the number of loaded modules.

        Available since Redis 4.0.0. Compiles to ``redis.call('MODULE', 'LIST')``.

        https://redis.io/docs/latest/commands/module-list/
        """

    def module_load(self, path: Any, /, *args: Any) -> Any:
        """Loads a module.

        Syntax::

            MODULE LOAD path [arg [arg ...]]

        Time complexity: O(1)

        Available since Redis 4.0.0. Compiles to ``redis.call('MODULE', 'LOAD', ...)``.

        https://redis.io/docs/latest/commands/module-load/
        """

    def module_loadex(self, path: Any, /, *args: Any) -> Any:
        """Loads a module using extended parameters.

        Syntax::

            MODULE LOADEX path [CONFIG name value [CONFIG name value ...]] [ARGS args [args
                ...]]

        Time complexity: O(1)

        Available since Redis 7.0.0. Compiles to ``redis.call('MODULE', 'LOADEX', ...)``.

        https://redis.io/docs/latest/commands/module-loadex/
        """

    def module_unload(self, name: Any, /) -> Any:
        """Unloads a module.

        Syntax::

            MODULE UNLOAD name

        Time complexity: O(1)

        Available since Redis 4.0.0. Compiles to ``redis.call('MODULE', 'UNLOAD', ...)``.

        https://redis.io/docs/latest/commands/module-unload/
        """

    def monitor(self) -> Any:
        """Listens for all requests received by the server in real-time.

        Syntax::

            MONITOR

        Available since Redis 1.0.0. Compiles to ``redis.call('MONITOR')``.

        https://redis.io/docs/latest/commands/monitor/
        """

    def move(self, key: Any, db: Any, /) -> Any:
        """Moves a key to another database.

        Syntax::

            MOVE key db

        Reply, one of:

        - key was moved
        - key wasn't moved

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('MOVE', ...)``.

        https://redis.io/docs/latest/commands/move/
        """

    def mset(self, *args: Any) -> Any:
        """Atomically creates or modifies the string values of one or more keys.

        Syntax::

            MSET key value [key value ...]

        Time complexity: O(N) where N is the number of keys to set.

        Available since Redis 1.0.1. Compiles to ``redis.call('MSET', ...)``.

        https://redis.io/docs/latest/commands/mset/
        """

    def msetex(self, numkeys: Any, /, *args: Any) -> Any:
        """Atomically sets multiple string keys with a shared expiration in a single operation.
        Supports flexible argument parsing where condition and expiration flags can appear in
        any order.

        Syntax::

            MSETEX numkeys key value [key value ...] [NX | XX] [EX seconds | PX milliseconds |
                EXAT unix-time-seconds | PXAT unix-time-milliseconds | KEEPTTL]

        Reply, one of:

        - No key was set (at least one key failed).
        - All the keys were set.

        Time complexity: O(N) where N is the number of keys to set.

        Available since Redis 8.4.0. Compiles to ``redis.call('MSETEX', ...)``.

        https://redis.io/docs/latest/commands/msetex/
        """

    def msetnx(self, *args: Any) -> Any:
        """Atomically modifies the string values of one or more keys only when all keys don't
        exist.

        Syntax::

            MSETNX key value [key value ...]

        Reply, one of:

        - No key was set (at least one key already existed).
        - All the keys were set.

        Time complexity: O(N) where N is the number of keys to set.

        Available since Redis 1.0.1. Compiles to ``redis.call('MSETNX', ...)``.

        https://redis.io/docs/latest/commands/msetnx/
        """

    def multi(self) -> Any:
        """Starts a transaction.

        Syntax::

            MULTI

        Time complexity: O(1)

        Available since Redis 1.2.0. Compiles to ``redis.call('MULTI')``.

        https://redis.io/docs/latest/commands/multi/
        """

    def object_encoding(self, key: Any, /) -> Any:
        """Returns the internal encoding of a Redis object.

        Syntax::

            OBJECT ENCODING key

        Reply, one of:

        - key doesn't exist
        - encoding of the object

        Time complexity: O(1)

        Available since Redis 2.2.3. Compiles to ``redis.call('OBJECT', 'ENCODING', ...)``.

        https://redis.io/docs/latest/commands/object-encoding/
        """

    def object_freq(self, key: Any, /) -> Any:
        """Returns the logarithmic access frequency counter of a Redis object.

        Syntax::

            OBJECT FREQ key

        Reply: the counter's value

        Time complexity: O(1)

        Available since Redis 4.0.0. Compiles to ``redis.call('OBJECT', 'FREQ', ...)``.

        https://redis.io/docs/latest/commands/object-freq/
        """

    def object_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            OBJECT HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('OBJECT', 'HELP')``.

        https://redis.io/docs/latest/commands/object-help/
        """

    def object_idletime(self, key: Any, /) -> Any:
        """Returns the time since the last access to a Redis object.

        Syntax::

            OBJECT IDLETIME key

        Reply: the idle time in seconds

        Time complexity: O(1)

        Available since Redis 2.2.3. Compiles to ``redis.call('OBJECT', 'IDLETIME', ...)``.

        https://redis.io/docs/latest/commands/object-idletime/
        """

    def object_refcount(self, key: Any, /) -> Any:
        """Returns the reference count of a value of a key.

        Syntax::

            OBJECT REFCOUNT key

        Reply: the number of references

        Time complexity: O(1)

        Available since Redis 2.2.3. Compiles to ``redis.call('OBJECT', 'REFCOUNT', ...)``.

        https://redis.io/docs/latest/commands/object-refcount/
        """

    def persist(self, key: Any, /) -> Any:
        """Removes the expiration time of a key.

        Syntax::

            PERSIST key

        Reply, one of:

        - Key does not exist or does not have an associated timeout.
        - The timeout has been removed.

        Time complexity: O(1)

        Available since Redis 2.2.0. Compiles to ``redis.call('PERSIST', ...)``.

        https://redis.io/docs/latest/commands/persist/
        """

    def pexpire(self, key: Any, milliseconds: Any, /, *args: Any) -> Any:
        """Sets the expiration time of a key in milliseconds.

        Syntax::

            PEXPIRE key milliseconds [NX | XX | GT | LT]

        Reply, one of:

        - The timeout was not set. e.g. key doesn't exist, or operation skipped due to the
          provided arguments.
        - The timeout was set.

        Time complexity: O(1)

        Available since Redis 2.6.0. Compiles to ``redis.call('PEXPIRE', ...)``.

        https://redis.io/docs/latest/commands/pexpire/
        """

    def pexpireat(self, key: Any, unix_time_milliseconds: Any, /, *args: Any) -> Any:
        """Sets the expiration time of a key to a Unix milliseconds timestamp.

        Syntax::

            PEXPIREAT key unix-time-milliseconds [NX | XX | GT | LT]

        Reply, one of:

        - The timeout was set.
        - The timeout was not set. e.g. key doesn't exist, or operation skipped due to the
          provided arguments.

        Time complexity: O(1)

        Available since Redis 2.6.0. Compiles to ``redis.call('PEXPIREAT', ...)``.

        https://redis.io/docs/latest/commands/pexpireat/
        """

    def pexpiretime(self, key: Any, /) -> Any:
        """Returns the expiration time of a key as a Unix milliseconds timestamp.

        Syntax::

            PEXPIRETIME key

        Reply, one of:

        - Expiration Unix timestamp in milliseconds.
        - The key exists but has no associated expiration time.
        - The key does not exist.

        Time complexity: O(1)

        Available since Redis 7.0.0. Compiles to ``redis.call('PEXPIRETIME', ...)``.

        https://redis.io/docs/latest/commands/pexpiretime/
        """

    def pfadd(self, key: Any, /, *args: Any) -> Any:
        """Adds elements to a HyperLogLog key. Creates the key if it doesn't exist.

        Syntax::

            PFADD key [element [element ...]]

        Reply, one of:

        - if at least 1 HyperLogLog internal register was altered
        - if no HyperLogLog internal register were altered

        Time complexity: O(1) to add every element.

        Available since Redis 2.8.9. Compiles to ``redis.call('PFADD', ...)``.

        https://redis.io/docs/latest/commands/pfadd/
        """

    def pfcount(self, key: Any, /, *args: Any) -> Any:
        """Returns the approximated cardinality of the set(s) observed by the HyperLogLog key(s).

        Syntax::

            PFCOUNT key [key ...]

        Reply: The approximated number of unique elements observed via PFADD

        Time complexity: O(1) with a very small average constant time when called with a single
        key. O(N) with N being the number of keys, and much bigger constant times, when called
        with multiple keys.

        Available since Redis 2.8.9. Compiles to ``redis.call('PFCOUNT', ...)``.

        https://redis.io/docs/latest/commands/pfcount/
        """

    def pfdebug(self, subcommand: Any, key: Any, /) -> Any:
        """Internal commands for debugging HyperLogLog values.

        Syntax::

            PFDEBUG subcommand key

        Time complexity: N/A

        Available since Redis 2.8.9. Compiles to ``redis.call('PFDEBUG', ...)``.

        https://redis.io/docs/latest/commands/pfdebug/
        """

    def pfmerge(self, destkey: Any, /, *args: Any) -> Any:
        """Merges one or more HyperLogLog values into a single key.

        Syntax::

            PFMERGE destkey [sourcekey [sourcekey ...]]

        Time complexity: O(N) to merge N HyperLogLogs, but with high constant times.

        Available since Redis 2.8.9. Compiles to ``redis.call('PFMERGE', ...)``.

        https://redis.io/docs/latest/commands/pfmerge/
        """

    def pfselftest(self) -> Any:
        """An internal command for testing HyperLogLog values.

        Syntax::

            PFSELFTEST

        Time complexity: N/A

        Available since Redis 2.8.9. Compiles to ``redis.call('PFSELFTEST')``.

        https://redis.io/docs/latest/commands/pfselftest/
        """

    def ping(self, *args: Any) -> Any:
        """Returns the server's liveliness response.

        Syntax::

            PING [message]

        Reply, one of:

        - Default reply.
        - Relay of given ``message``.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('PING', ...)``.

        https://redis.io/docs/latest/commands/ping/
        """

    def psetex(self, key: Any, milliseconds: Any, value: Any, /) -> Any:
        """Sets both string value and expiration time in milliseconds of a key. The key is created
        if it doesn't exist.

        Deprecated since Redis 2.6.12, replaced by ``SET`` with the ``PX`` argument.

        Syntax::

            PSETEX key milliseconds value

        Time complexity: O(1)

        Available since Redis 2.6.0. Compiles to ``redis.call('PSETEX', ...)``.

        https://redis.io/docs/latest/commands/psetex/
        """

    def psubscribe(self, pattern: Any, /, *args: Any) -> Any:
        """Listens for messages published to channels that match one or more patterns.

        Syntax::

            PSUBSCRIBE pattern [pattern ...]

        Time complexity: O(N) where N is the number of patterns to subscribe to.

        Available since Redis 2.0.0. Compiles to ``redis.call('PSUBSCRIBE', ...)``.

        https://redis.io/docs/latest/commands/psubscribe/
        """

    def psync(self, replicationid: Any, offset: Any, /, *args: Any) -> Any:
        """An internal command used in replication.

        Syntax::

            PSYNC replicationid offset

        Available since Redis 2.8.0. Compiles to ``redis.call('PSYNC', ...)``.

        https://redis.io/docs/latest/commands/psync/
        """

    def pttl(self, key: Any, /) -> Any:
        """Returns the expiration time in milliseconds of a key.

        Syntax::

            PTTL key

        Reply, one of:

        - TTL in milliseconds.
        - The key exists but has no associated expire.
        - The key does not exist.

        Time complexity: O(1)

        Available since Redis 2.6.0. Compiles to ``redis.call('PTTL', ...)``.

        https://redis.io/docs/latest/commands/pttl/
        """

    def publish(self, channel: Any, message: Any, /) -> Any:
        """Posts a message to a channel.

        Syntax::

            PUBLISH channel message

        Reply: the number of clients that received the message. Note that in a Redis Cluster,
        only clients that are connected to the same node as the publishing client are included
        in the count

        Time complexity: O(N+M) where N is the number of clients subscribed to the receiving
        channel and M is the total number of subscribed patterns (by any client).

        Available since Redis 2.0.0. Compiles to ``redis.call('PUBLISH', ...)``.

        https://redis.io/docs/latest/commands/publish/
        """

    def pubsub_channels(self, *args: Any) -> Any:
        """Returns the active channels.

        Syntax::

            PUBSUB CHANNELS [pattern]

        Reply: a list of active channels, optionally matching the specified pattern

        Time complexity: O(N) where N is the number of active channels, and assuming constant
        time pattern matching (relatively short channels and patterns)

        Available since Redis 2.8.0. Compiles to ``redis.call('PUBSUB', 'CHANNELS', ...)``.

        https://redis.io/docs/latest/commands/pubsub-channels/
        """

    def pubsub_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            PUBSUB HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('PUBSUB', 'HELP')``.

        https://redis.io/docs/latest/commands/pubsub-help/
        """

    def pubsub_numpat(self) -> Any:
        """Returns a count of unique pattern subscriptions.

        Syntax::

            PUBSUB NUMPAT

        Reply: the number of patterns all the clients are subscribed to

        Time complexity: O(1)

        Available since Redis 2.8.0. Compiles to ``redis.call('PUBSUB', 'NUMPAT')``.

        https://redis.io/docs/latest/commands/pubsub-numpat/
        """

    def pubsub_numsub(self, *args: Any) -> Any:
        """Returns a count of subscribers to channels.

        Syntax::

            PUBSUB NUMSUB [channel [channel ...]]

        Reply: the number of subscribers per channel, each even element (including 0th) is
        channel name, each odd element is the number of subscribers

        Time complexity: O(N) for the NUMSUB subcommand, where N is the number of requested
        channels

        Available since Redis 2.8.0. Compiles to ``redis.call('PUBSUB', 'NUMSUB', ...)``.

        https://redis.io/docs/latest/commands/pubsub-numsub/
        """

    def pubsub_shardchannels(self, *args: Any) -> Any:
        """Returns the active shard channels.

        Syntax::

            PUBSUB SHARDCHANNELS [pattern]

        Reply: a list of active channels, optionally matching the specified pattern

        Time complexity: O(N) where N is the number of active shard channels, and assuming
        constant time pattern matching (relatively short shard channels).

        Available since Redis 7.0.0. Compiles to ``redis.call('PUBSUB', 'SHARDCHANNELS', ...)``.

        https://redis.io/docs/latest/commands/pubsub-shardchannels/
        """

    def pubsub_shardnumsub(self, *args: Any) -> Any:
        """Returns the count of subscribers of shard channels.

        Syntax::

            PUBSUB SHARDNUMSUB [shardchannel [shardchannel ...]]

        Reply: the number of subscribers per shard channel, each even element (including 0th) is
        channel name, each odd element is the number of subscribers

        Time complexity: O(N) for the SHARDNUMSUB subcommand, where N is the number of requested
        shard channels

        Available since Redis 7.0.0. Compiles to ``redis.call('PUBSUB', 'SHARDNUMSUB', ...)``.

        https://redis.io/docs/latest/commands/pubsub-shardnumsub/
        """

    def punsubscribe(self, *args: Any) -> Any:
        """Stops listening to messages published to channels that match one or more patterns.

        Syntax::

            PUNSUBSCRIBE [pattern [pattern ...]]

        Time complexity: O(N) where N is the number of patterns to unsubscribe.

        Available since Redis 2.0.0. Compiles to ``redis.call('PUNSUBSCRIBE', ...)``.

        https://redis.io/docs/latest/commands/punsubscribe/
        """

    def quit(self, *args: Any) -> Any:
        """Closes the connection.

        Deprecated since Redis 7.2.0, replaced by just closing the connection.

        Syntax::

            QUIT

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('QUIT', ...)``.

        https://redis.io/docs/latest/commands/quit/
        """

    def randomkey(self) -> Any:
        """Returns a random key name from the database.

        Syntax::

            RANDOMKEY

        Reply, one of:

        - when the database is empty
        - random key in db

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('RANDOMKEY')``.

        https://redis.io/docs/latest/commands/randomkey/
        """

    def readonly(self) -> Any:
        """Enables read-only queries for a connection to a Redis Cluster replica node.

        Syntax::

            READONLY

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('READONLY')``.

        https://redis.io/docs/latest/commands/readonly/
        """

    def readwrite(self) -> Any:
        """Enables read-write queries for a connection to a Reids Cluster replica node.

        Syntax::

            READWRITE

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('READWRITE')``.

        https://redis.io/docs/latest/commands/readwrite/
        """

    def rename(self, key: Any, newkey: Any, /) -> Any:
        """Renames a key and overwrites the destination.

        Syntax::

            RENAME key newkey

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('RENAME', ...)``.

        https://redis.io/docs/latest/commands/rename/
        """

    def renamenx(self, key: Any, newkey: Any, /) -> Any:
        """Renames a key only when the target key name doesn't exist.

        Syntax::

            RENAMENX key newkey

        Reply, one of:

        - key was renamed to newkey
        - new key already exists

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('RENAMENX', ...)``.

        https://redis.io/docs/latest/commands/renamenx/
        """

    def replconf(self, *args: Any) -> Any:
        """An internal command for configuring the replication stream.

        Syntax::

            REPLCONF

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('REPLCONF', ...)``.

        https://redis.io/docs/latest/commands/replconf/
        """

    def replicaof(self, *args: Any) -> Any:
        """Configures a server as replica of another, or promotes it to a master.

        Syntax::

            REPLICAOF <host port | NO ONE>

        Reply: replicaOf status

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('REPLICAOF', ...)``.

        https://redis.io/docs/latest/commands/replicaof/
        """

    def reset(self) -> Any:
        """Resets the connection.

        Syntax::

            RESET

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('RESET')``.

        https://redis.io/docs/latest/commands/reset/
        """

    def restore(self, key: Any, ttl: Any, serialized_value: Any, /, *args: Any) -> Any:
        """Creates a key from the serialized representation of a value.

        Syntax::

            RESTORE key ttl serialized-value [REPLACE] [ABSTTL] [IDLETIME seconds] [FREQ
                frequency]

        Time complexity: O(1) to create the new key and additional O(N*M) to reconstruct the
        serialized value, where N is the number of Redis objects composing the value and M their
        average size. For small string values the time complexity is thus O(1)+O(1*M) where M is
        small, so simply O(1). However for sorted set values the complexity is O(N*M*log(N))
        because inserting values into sorted sets is O(log(N)).

        Available since Redis 2.6.0. Compiles to ``redis.call('RESTORE', ...)``.

        https://redis.io/docs/latest/commands/restore/
        """

    def role(self) -> Any:
        """Returns the replication role.

        Syntax::

            ROLE

        Time complexity: O(1)

        Available since Redis 2.8.12. Compiles to ``redis.call('ROLE')``.

        https://redis.io/docs/latest/commands/role/
        """

    def rpop(self, key: Any, /, *args: Any) -> Any:
        """Returns and removes the last elements of a list. Deletes the list if the last element
        was popped.

        Syntax::

            RPOP key [count]

        Reply, one of:

        - Key does not exist.
        - When 'COUNT' was not given, the value of the last element.
        - When 'COUNT' was given, list of popped elements.

        Time complexity: O(N) where N is the number of elements returned

        Available since Redis 1.0.0. Compiles to ``redis.call('RPOP', ...)``.

        https://redis.io/docs/latest/commands/rpop/
        """

    def rpoplpush(self, source: Any, destination: Any, /) -> Any:
        """Returns the last element of a list after removing and pushing it to another list.
        Deletes the list if the last element was popped.

        Deprecated since Redis 6.2.0, replaced by ``LMOVE`` with the ``RIGHT`` and ``LEFT``
        arguments.

        Syntax::

            RPOPLPUSH source destination

        Reply, one of:

        - The element being popped and pushed.
        - Source list is empty.

        Time complexity: O(1)

        Available since Redis 1.2.0. Compiles to ``redis.call('RPOPLPUSH', ...)``.

        https://redis.io/docs/latest/commands/rpoplpush/
        """

    def rpush(self, key: Any, element: Any, /, *args: Any) -> Any:
        """Appends one or more elements to a list. Creates the key if it doesn't exist.

        Syntax::

            RPUSH key element [element ...]

        Reply: Length of the list after the push operations.

        Time complexity: O(1) for each element added, so O(N) to add N elements when the command
        is called with multiple arguments.

        Available since Redis 1.0.0. Compiles to ``redis.call('RPUSH', ...)``.

        https://redis.io/docs/latest/commands/rpush/
        """

    def rpushx(self, key: Any, element: Any, /, *args: Any) -> Any:
        """Appends an element to a list only when the list exists.

        Syntax::

            RPUSHX key element [element ...]

        Reply: Length of the list after the push operation.

        Time complexity: O(1) for each element added, so O(N) to add N elements when the command
        is called with multiple arguments.

        Available since Redis 2.2.0. Compiles to ``redis.call('RPUSHX', ...)``.

        https://redis.io/docs/latest/commands/rpushx/
        """

    def sadd(self, key: Any, member: Any, /, *args: Any) -> Any:
        """Adds one or more members to a set. Creates the key if it doesn't exist.

        Syntax::

            SADD key member [member ...]

        Reply: Number of elements that were added to the set, not including all the elements
        already present in the set.

        Time complexity: O(1) for each element added, so O(N) to add N elements when the command
        is called with multiple arguments.

        Available since Redis 1.0.0. Compiles to ``redis.call('SADD', ...)``.

        https://redis.io/docs/latest/commands/sadd/
        """

    def save(self) -> Any:
        """Synchronously saves the database(s) to disk.

        Syntax::

            SAVE

        Time complexity: O(N) where N is the total number of keys in all databases

        Available since Redis 1.0.0. Compiles to ``redis.call('SAVE')``.

        https://redis.io/docs/latest/commands/save/
        """

    def scan(self, cursor: Any, /, *args: Any) -> Any:
        """Iterates over the key names in the database.

        Syntax::

            SCAN cursor [MATCH pattern] [COUNT count] [TYPE type]

        Reply: cursor and scan response in array form

        Time complexity: O(1) for every call. O(N) for a complete iteration, including enough
        command calls for the cursor to return back to 0. N is the number of elements inside the
        collection.

        Available since Redis 2.8.0. Compiles to ``redis.call('SCAN', ...)``.

        https://redis.io/docs/latest/commands/scan/
        """

    def scard(self, key: Any, /) -> Any:
        """Returns the number of members in a set.

        Syntax::

            SCARD key

        Reply: The cardinality (number of elements) of the set, or 0 if key does not exist.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('SCARD', ...)``.

        https://redis.io/docs/latest/commands/scard/
        """

    def script_debug(self, *args: Any) -> Any:
        """Sets the debug mode of server-side Lua scripts.

        Syntax::

            SCRIPT DEBUG <YES | SYNC | NO>

        Time complexity: O(1)

        Available since Redis 3.2.0. Compiles to ``redis.call('SCRIPT', 'DEBUG', ...)``.

        https://redis.io/docs/latest/commands/script-debug/
        """

    def script_exists(self, sha1: Any, /, *args: Any) -> Any:
        """Determines whether server-side Lua scripts exist in the script cache.

        Syntax::

            SCRIPT EXISTS sha1 [sha1 ...]

        Reply: An array of integers that correspond to the specified SHA1 digest arguments.

        Time complexity: O(N) with N being the number of scripts to check (so checking a single
        script is an O(1) operation).

        Available since Redis 2.6.0. Compiles to ``redis.call('SCRIPT', 'EXISTS', ...)``.

        https://redis.io/docs/latest/commands/script-exists/
        """

    def script_flush(self, *args: Any) -> Any:
        """Removes all server-side Lua scripts from the script cache.

        Syntax::

            SCRIPT FLUSH [ASYNC | SYNC]

        Time complexity: O(N) with N being the number of scripts in cache

        Available since Redis 2.6.0. Compiles to ``redis.call('SCRIPT', 'FLUSH', ...)``.

        https://redis.io/docs/latest/commands/script-flush/
        """

    def script_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            SCRIPT HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('SCRIPT', 'HELP')``.

        https://redis.io/docs/latest/commands/script-help/
        """

    def script_kill(self) -> Any:
        """Terminates a server-side Lua script during execution.

        Syntax::

            SCRIPT KILL

        Time complexity: O(1)

        Available since Redis 2.6.0. Compiles to ``redis.call('SCRIPT', 'KILL')``.

        https://redis.io/docs/latest/commands/script-kill/
        """

    def script_load(self, script: Any, /) -> Any:
        """Loads a server-side Lua script to the script cache.

        Syntax::

            SCRIPT LOAD script

        Reply: The SHA1 digest of the script added into the script cache

        Time complexity: O(N) with N being the length in bytes of the script body.

        Available since Redis 2.6.0. Compiles to ``redis.call('SCRIPT', 'LOAD', ...)``.

        https://redis.io/docs/latest/commands/script-load/
        """

    def sdiff(self, key: Any, /, *args: Any) -> Any:
        """Returns the difference of multiple sets.

        Syntax::

            SDIFF key [key ...]

        Reply: List with the members of the resulting set.

        Time complexity: O(N) where N is the total number of elements in all given sets.

        Available since Redis 1.0.0. Compiles to ``redis.call('SDIFF', ...)``.

        https://redis.io/docs/latest/commands/sdiff/
        """

    def sdiffcard(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns the number of members of the difference between the first set and all successive
        sets.

        Syntax::

            SDIFFCARD numkeys key [key ...] [LIMIT limit]

        Reply: Number of the elements in the resulting difference.

        Time complexity: O(N) where N is the total number of elements in all given sets.

        Available since Redis 8.10.0. Compiles to ``redis.call('SDIFFCARD', ...)``.

        https://redis.io/docs/latest/commands/sdiffcard/
        """

    def sdiffstore(self, destination: Any, key: Any, /, *args: Any) -> Any:
        """Stores the difference of multiple sets in a key.

        Syntax::

            SDIFFSTORE destination key [key ...]

        Reply: Number of the elements in the resulting set.

        Time complexity: O(N) where N is the total number of elements in all given sets.

        Available since Redis 1.0.0. Compiles to ``redis.call('SDIFFSTORE', ...)``.

        https://redis.io/docs/latest/commands/sdiffstore/
        """

    def select(self, index: Any, /) -> Any:
        """Changes the selected database.

        Syntax::

            SELECT index

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('SELECT', ...)``.

        https://redis.io/docs/latest/commands/select/
        """

    def sentinel_ckquorum(self, master_name: Any, /) -> Any:
        """Checks for a Redis Sentinel quorum.

        Syntax::

            SENTINEL CKQUORUM master-name

        Reply: Returns OK if the current Sentinel configuration is able to reach the quorum
        needed to failover a master, and the majority needed to authorize the failover.

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'CKQUORUM', ...)``.
        """

    def sentinel_config(self, *args: Any) -> Any:
        """Configures Redis Sentinel.

        Syntax::

            SENTINEL CONFIG <SET parameter value [parameter value ...] | GET parameter
                [parameter ...]>

        Reply, one of:

        - When 'SENTINEL-CONFIG GET' is called, returns a map.
        - When 'SENTINEL-CONFIG SET' is called, returns OK on success.

        Time complexity: O(N) when N is the number of configuration parameters provided

        Available since Redis 6.2.0. Compiles to ``redis.call('SENTINEL', 'CONFIG', ...)``.
        """

    def sentinel_debug(self, *args: Any) -> Any:
        """Lists or updates the current configurable parameters of Redis Sentinel.

        Syntax::

            SENTINEL DEBUG [parameter value [parameter value ...]]

        Reply, one of:

        - The configuration update was successful.
        - List of configurable time parameters and their values (milliseconds).

        Time complexity: O(N) where N is the number of configurable parameters

        Available since Redis 7.0.0. Compiles to ``redis.call('SENTINEL', 'DEBUG', ...)``.
        """

    def sentinel_failover(self, master_name: Any, /) -> Any:
        """Forces a Redis Sentinel failover.

        Syntax::

            SENTINEL FAILOVER master-name

        Reply: Force a fail over as if the master was not reachable, and without asking for
        agreement to other Sentinels.

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'FAILOVER', ...)``.
        """

    def sentinel_flushconfig(self) -> Any:
        """Rewrites the Redis Sentinel configuration file.

        Syntax::

            SENTINEL FLUSHCONFIG

        Reply: Force Sentinel to rewrite its configuration on disk, including the current
        Sentinel state.

        Time complexity: O(1)

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'FLUSHCONFIG')``.
        """

    def sentinel_get_master_addr_by_name(self, master_name: Any, /) -> Any:
        """Returns the port and address of a master Redis instance.

        Syntax::

            SENTINEL GET-MASTER-ADDR-BY-NAME master-name

        Time complexity: O(1)

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'GET-MASTER-ADDR-BY-
        NAME', ...)``.
        """

    def sentinel_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            SENTINEL HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('SENTINEL', 'HELP')``.
        """

    def sentinel_info_cache(self, nodename: Any, /, *args: Any) -> Any:
        """Returns the cached ``INFO`` replies from the deployment's instances.

        Syntax::

            SENTINEL INFO-CACHE nodename [nodename ...]

        Reply: This is actually a map, the odd entries are a master name, and the even entries
        are the last cached INFO output from that master and all its replicas.

        Time complexity: O(N) where N is the number of instances

        Available since Redis 3.2.0. Compiles to ``redis.call('SENTINEL', 'INFO-CACHE', ...)``.
        """

    def sentinel_is_master_down_by_addr(
        self, ip: Any, port: Any, current_epoch: Any, runid: Any, /
    ) -> Any:
        """Determines whether a master Redis instance is down.

        Syntax::

            SENTINEL IS-MASTER-DOWN-BY-ADDR ip port current-epoch runid

        Time complexity: O(1)

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'IS-MASTER-DOWN-BY-
        ADDR', ...)``.
        """

    def sentinel_master(self, master_name: Any, /) -> Any:
        """Returns the state of a master Redis instance.

        Syntax::

            SENTINEL MASTER master-name

        Reply: The state and info of the specified master.

        Time complexity: O(1)

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'MASTER', ...)``.
        """

    def sentinel_masters(self) -> Any:
        """Returns a list of monitored Redis masters.

        Syntax::

            SENTINEL MASTERS

        Reply: List of monitored Redis masters, and their state.

        Time complexity: O(N) where N is the number of masters

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'MASTERS')``.
        """

    def sentinel_monitor(self, name: Any, ip: Any, port: Any, quorum: Any, /) -> Any:
        """Starts monitoring.

        Syntax::

            SENTINEL MONITOR name ip port quorum

        Time complexity: O(1)

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'MONITOR', ...)``.
        """

    def sentinel_myid(self) -> Any:
        """Returns the Redis Sentinel instance ID.

        Syntax::

            SENTINEL MYID

        Reply: Node ID of the sentinel instance.

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('SENTINEL', 'MYID')``.
        """

    def sentinel_pending_scripts(self) -> Any:
        """Returns information about pending scripts for Redis Sentinel.

        Syntax::

            SENTINEL PENDING-SCRIPTS

        Reply: List of pending scripts.

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'PENDING-SCRIPTS')``.
        """

    def sentinel_remove(self, master_name: Any, /) -> Any:
        """Stops monitoring.

        Syntax::

            SENTINEL REMOVE master-name

        Time complexity: O(1)

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'REMOVE', ...)``.
        """

    def sentinel_replicas(self, master_name: Any, /) -> Any:
        """Returns a list of the monitored Redis replicas.

        Syntax::

            SENTINEL REPLICAS master-name

        Reply: List of replicas for this master, and their state.

        Time complexity: O(N) where N is the number of replicas

        Available since Redis 5.0.0. Compiles to ``redis.call('SENTINEL', 'REPLICAS', ...)``.
        """

    def sentinel_reset(self, pattern: Any, /) -> Any:
        """Resets Redis masters by name matching a pattern.

        Syntax::

            SENTINEL RESET pattern

        Reply: The number of masters that were reset.

        Time complexity: O(N) where N is the number of monitored masters

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'RESET', ...)``.
        """

    def sentinel_sentinels(self, master_name: Any, /) -> Any:
        """Returns a list of Sentinel instances.

        Syntax::

            SENTINEL SENTINELS master-name

        Reply: List of sentinel instances, and their state.

        Time complexity: O(N) where N is the number of Sentinels

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'SENTINELS', ...)``.
        """

    def sentinel_set(self, master_name: Any, /, *args: Any) -> Any:
        """Changes the configuration of a monitored Redis master.

        Syntax::

            SENTINEL SET master-name option value [option value ...]

        Time complexity: O(1)

        Available since Redis 2.8.4. Compiles to ``redis.call('SENTINEL', 'SET', ...)``.
        """

    def sentinel_simulate_failure(self, *args: Any) -> Any:
        """Simulates failover scenarios.

        Syntax::

            SENTINEL SIMULATE-FAILURE [<CRASH-AFTER-ELECTION | CRASH-AFTER-PROMOTION | HELP>
                [<CRASH-AFTER-ELECTION | CRASH-AFTER-PROMOTION | HELP> ...]]

        Reply, one of:

        - The simulated flag was set.
        - Supported simulates flags. Returned in case ``HELP`` was used.

        Available since Redis 3.2.0. Compiles to ``redis.call('SENTINEL', 'SIMULATE-FAILURE',
        ...)``.
        """

    def sentinel_slaves(self, master_name: Any, /) -> Any:
        """Returns a list of the monitored replicas.

        Deprecated since Redis 5.0.0, replaced by ``SENTINEL REPLICAS``.

        Syntax::

            SENTINEL SLAVES master-name

        Reply: List of monitored replicas, and their state.

        Time complexity: O(N) where N is the number of replicas.

        Available since Redis 2.8.0. Compiles to ``redis.call('SENTINEL', 'SLAVES', ...)``.
        """

    def set(self, key: Any, value: Any, /, *args: Any) -> Any:
        """Sets the string value of a key, ignoring its type. The key is created if it doesn't
        exist.

        Syntax::

            SET key value [NX | XX | IFEQ ifeq-value | IFNE ifne-value | IFDEQ ifdeq-digest |
                IFDNE ifdne-digest] [GET] [EX seconds | PX milliseconds | EXAT unix-time-seconds
                | PXAT unix-time-milliseconds | KEEPTTL]

        Reply, one of:

        - ``GET`` not given: Operation was aborted (conflict with one of the
          ``XX``/``NX``/``IFEQ``/``IFDEQ`` options or condition not met).
        - ``GET`` not given: The key was set.
        - ``GET`` given: The key didn't exist before the ``SET``
        - ``GET`` given: The previous value of the key

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('SET', ...)``.

        https://redis.io/docs/latest/commands/set/
        """

    def setbit(self, key: Any, offset: Any, value: Any, /) -> Any:
        """Sets or clears the bit at offset of the string value. Creates the key if it doesn't
        exist.

        Syntax::

            SETBIT key offset value

        Reply: The original bit value stored at offset.

        Time complexity: O(1)

        Available since Redis 2.2.0. Compiles to ``redis.call('SETBIT', ...)``.

        https://redis.io/docs/latest/commands/setbit/
        """

    def setex(self, key: Any, seconds: Any, value: Any, /) -> Any:
        """Sets the string value and expiration time of a key. Creates the key if it doesn't exist.

        Deprecated since Redis 2.6.12, replaced by ``SET`` with the ``EX`` argument.

        Syntax::

            SETEX key seconds value

        Time complexity: O(1)

        Available since Redis 2.0.0. Compiles to ``redis.call('SETEX', ...)``.

        https://redis.io/docs/latest/commands/setex/
        """

    def setnx(self, key: Any, value: Any, /) -> Any:
        """Set the string value of a key only when the key doesn't exist.

        Deprecated since Redis 2.6.12, replaced by ``SET`` with the ``NX`` argument.

        Syntax::

            SETNX key value

        Reply, one of:

        - The key was set.
        - The key was not set.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('SETNX', ...)``.

        https://redis.io/docs/latest/commands/setnx/
        """

    def setrange(self, key: Any, offset: Any, value: Any, /) -> Any:
        """Overwrites a part of a string value with another by an offset. Creates the key if it
        doesn't exist.

        Syntax::

            SETRANGE key offset value

        Reply: Length of the string after it was modified by the command.

        Time complexity: O(1), not counting the time taken to copy the new string in place.
        Usually, this string is very small so the amortized complexity is O(1). Otherwise,
        complexity is O(M) with M being the length of the value argument.

        Available since Redis 2.2.0. Compiles to ``redis.call('SETRANGE', ...)``.

        https://redis.io/docs/latest/commands/setrange/
        """

    def sflush(self, *args: Any) -> Any:
        """Remove all keys from selected range of slots.

        Syntax::

            SFLUSH slot-start slot-last [slot-start slot-last ...] [ASYNC | SYNC]

        Reply: List of slot ranges

        Time complexity: O(N)+O(k) where N is the number of keys and k is the number of slots.

        Available since Redis 8.0.0. Compiles to ``redis.call('SFLUSH', ...)``.

        https://redis.io/docs/latest/commands/sflush/
        """

    def shutdown(self, *args: Any) -> Any:
        """Synchronously saves the database(s) to disk and shuts down the Redis server.

        Syntax::

            SHUTDOWN [NOSAVE | SAVE] [NOW] [FORCE] [ABORT]

        Reply: OK if ABORT was specified and shutdown was aborted. On successful shutdown,
        nothing is returned since the server quits and the connection is closed. On failure, an
        error is returned.

        Time complexity: O(N) when saving, where N is the total number of keys in all databases
        when saving data, otherwise O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('SHUTDOWN', ...)``.

        https://redis.io/docs/latest/commands/shutdown/
        """

    def sinter(self, key: Any, /, *args: Any) -> Any:
        """Returns the intersect of multiple sets.

        Syntax::

            SINTER key [key ...]

        Reply: List with the members of the resulting set.

        Time complexity: O(N*M) worst case where N is the cardinality of the smallest set and M
        is the number of sets.

        Available since Redis 1.0.0. Compiles to ``redis.call('SINTER', ...)``.

        https://redis.io/docs/latest/commands/sinter/
        """

    def sintercard(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns the number of members of the intersect of multiple sets.

        Syntax::

            SINTERCARD numkeys key [key ...] [LIMIT limit]

        Reply: Number of the elements in the resulting intersection.

        Time complexity: O(N*M) worst case where N is the cardinality of the smallest set and M
        is the number of sets.

        Available since Redis 7.0.0. Compiles to ``redis.call('SINTERCARD', ...)``.

        https://redis.io/docs/latest/commands/sintercard/
        """

    def sinterstore(self, destination: Any, key: Any, /, *args: Any) -> Any:
        """Stores the intersect of multiple sets in a key.

        Syntax::

            SINTERSTORE destination key [key ...]

        Reply: Number of the elements in the result set.

        Time complexity: O(N*M) worst case where N is the cardinality of the smallest set and M
        is the number of sets.

        Available since Redis 1.0.0. Compiles to ``redis.call('SINTERSTORE', ...)``.

        https://redis.io/docs/latest/commands/sinterstore/
        """

    def sismember(self, key: Any, member: Any, /) -> Any:
        """Determines whether a member belongs to a set.

        Syntax::

            SISMEMBER key member

        Reply, one of:

        - The element is not a member of the set, or the key does not exist.
        - The element is a member of the set.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('SISMEMBER', ...)``.

        https://redis.io/docs/latest/commands/sismember/
        """

    def slaveof(self, *args: Any) -> Any:
        """Sets a Redis server as a replica of another, or promotes it to being a master.

        Deprecated since Redis 5.0.0, replaced by ``REPLICAOF``.

        Syntax::

            SLAVEOF <host port | NO ONE>

        Reply: slaveOf status

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('SLAVEOF', ...)``.

        https://redis.io/docs/latest/commands/slaveof/
        """

    def slowlog_get(self, *args: Any) -> Any:
        """Returns the slow log's entries.

        Syntax::

            SLOWLOG GET [count]

        Reply: Entries from the slow log in chronological order.

        Time complexity: O(N) where N is the number of entries returned

        Available since Redis 2.2.12. Compiles to ``redis.call('SLOWLOG', 'GET', ...)``.

        https://redis.io/docs/latest/commands/slowlog-get/
        """

    def slowlog_help(self) -> Any:
        """Show helpful text about the different subcommands

        Syntax::

            SLOWLOG HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('SLOWLOG', 'HELP')``.

        https://redis.io/docs/latest/commands/slowlog-help/
        """

    def slowlog_len(self) -> Any:
        """Returns the number of entries in the slow log.

        Syntax::

            SLOWLOG LEN

        Reply: Number of entries in the slow log.

        Time complexity: O(1)

        Available since Redis 2.2.12. Compiles to ``redis.call('SLOWLOG', 'LEN')``.

        https://redis.io/docs/latest/commands/slowlog-len/
        """

    def slowlog_reset(self) -> Any:
        """Clears all entries from the slow log.

        Syntax::

            SLOWLOG RESET

        Time complexity: O(N) where N is the number of entries in the slowlog

        Available since Redis 2.2.12. Compiles to ``redis.call('SLOWLOG', 'RESET')``.

        https://redis.io/docs/latest/commands/slowlog-reset/
        """

    def smembers(self, key: Any, /) -> Any:
        """Returns all members of a set.

        Syntax::

            SMEMBERS key

        Reply: All elements of the set.

        Time complexity: O(N) where N is the set cardinality.

        Available since Redis 1.0.0. Compiles to ``redis.call('SMEMBERS', ...)``.

        https://redis.io/docs/latest/commands/smembers/
        """

    def smismember(self, key: Any, member: Any, /, *args: Any) -> Any:
        """Determines whether multiple members belong to a set.

        Syntax::

            SMISMEMBER key member [member ...]

        Reply: List representing the membership of the given elements, in the same order as they
        are requested.

        Time complexity: O(N) where N is the number of elements being checked for membership

        Available since Redis 6.2.0. Compiles to ``redis.call('SMISMEMBER', ...)``.

        https://redis.io/docs/latest/commands/smismember/
        """

    def smove(self, source: Any, destination: Any, member: Any, /) -> Any:
        """Moves a member from one set to another.

        Syntax::

            SMOVE source destination member

        Reply, one of:

        - Element is moved.
        - The element is not a member of source and no operation was performed.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('SMOVE', ...)``.

        https://redis.io/docs/latest/commands/smove/
        """

    def sort(self, key: Any, /, *args: Any) -> Any:
        """Sorts the elements in a list, a set, or a sorted set, optionally storing the result.

        Syntax::

            SORT key [BY pattern] [LIMIT offset count] [GET pattern [GET pattern ...]] [ASC |
                DESC] [ALPHA] [STORE destination]

        Reply, one of:

        - when the store option is specified the command returns the number of sorted elements
          in the destination list
        - when not passing the store option the command returns a list of sorted elements

        Time complexity: O(N+M*log(M)) where N is the number of elements in the list or set to
        sort, and M the number of returned elements. When the elements are not sorted,
        complexity is O(N).

        Available since Redis 1.0.0. Compiles to ``redis.call('SORT', ...)``.

        https://redis.io/docs/latest/commands/sort/
        """

    def sort_ro(self, key: Any, /, *args: Any) -> Any:
        """Returns the sorted elements of a list, a set, or a sorted set.

        Syntax::

            SORT_RO key [BY pattern] [LIMIT offset count] [GET pattern [GET pattern ...]] [ASC |
                DESC] [ALPHA]

        Reply: a list of sorted elements

        Time complexity: O(N+M*log(M)) where N is the number of elements in the list or set to
        sort, and M the number of returned elements. When the elements are not sorted,
        complexity is O(N).

        Available since Redis 7.0.0. Compiles to ``redis.call('SORT_RO', ...)``.

        https://redis.io/docs/latest/commands/sort_ro/
        """

    def spop(self, key: Any, /, *args: Any) -> Any:
        """Returns one or more random members from a set after removing them. Deletes the set if
        the last member was popped.

        Syntax::

            SPOP key [count]

        Reply, one of:

        - The key does not exist.
        - The removed member when 'COUNT' is not given.
        - List to the removed members when 'COUNT' is given.

        Time complexity: Without the count argument O(1), otherwise O(N) where N is the value of
        the passed count.

        Available since Redis 1.0.0. Compiles to ``redis.call('SPOP', ...)``.

        https://redis.io/docs/latest/commands/spop/
        """

    def spublish(self, shardchannel: Any, message: Any, /) -> Any:
        """Post a message to a shard channel

        Syntax::

            SPUBLISH shardchannel message

        Reply: the number of clients that received the message. Note that in a Redis Cluster,
        only clients that are connected to the same node as the publishing client are included
        in the count

        Time complexity: O(N) where N is the number of clients subscribed to the receiving shard
        channel.

        Available since Redis 7.0.0. Compiles to ``redis.call('SPUBLISH', ...)``.

        https://redis.io/docs/latest/commands/spublish/
        """

    def srandmember(self, key: Any, /, *args: Any) -> Any:
        """Get one or multiple random members from a set

        Syntax::

            SRANDMEMBER key [count]

        Reply, one of:

        - In case ``count`` is not given and key doesn't exist
        - In case ``count`` is not given, randomly selected element
        - In case ``count`` is given, an array of elements
        - In case ``count`` is given and key doesn't exist

        Time complexity: Without the count argument O(1), otherwise O(N) where N is the absolute
        value of the passed count.

        Available since Redis 1.0.0. Compiles to ``redis.call('SRANDMEMBER', ...)``.

        https://redis.io/docs/latest/commands/srandmember/
        """

    def srem(self, key: Any, member: Any, /, *args: Any) -> Any:
        """Removes one or more members from a set. Deletes the set if the last member was removed.

        Syntax::

            SREM key member [member ...]

        Reply: Number of members that were removed from the set, not including non existing
        members.

        Time complexity: O(N) where N is the number of members to be removed.

        Available since Redis 1.0.0. Compiles to ``redis.call('SREM', ...)``.

        https://redis.io/docs/latest/commands/srem/
        """

    def sscan(self, key: Any, cursor: Any, /, *args: Any) -> Any:
        """Iterates over members of a set.

        Syntax::

            SSCAN key cursor [MATCH pattern] [COUNT count]

        Reply: cursor and scan response in array form

        Time complexity: O(1) for every call. O(N) for a complete iteration, including enough
        command calls for the cursor to return back to 0. N is the number of elements inside the
        collection.

        Available since Redis 2.8.0. Compiles to ``redis.call('SSCAN', ...)``.

        https://redis.io/docs/latest/commands/sscan/
        """

    def ssubscribe(self, shardchannel: Any, /, *args: Any) -> Any:
        """Listens for messages published to shard channels.

        Syntax::

            SSUBSCRIBE shardchannel [shardchannel ...]

        Time complexity: O(N) where N is the number of shard channels to subscribe to.

        Available since Redis 7.0.0. Compiles to ``redis.call('SSUBSCRIBE', ...)``.

        https://redis.io/docs/latest/commands/ssubscribe/
        """

    def strlen(self, key: Any, /) -> Any:
        """Returns the length of a string value.

        Syntax::

            STRLEN key

        Reply: The length of the string value stored at key, or 0 when key does not exist.

        Time complexity: O(1)

        Available since Redis 2.2.0. Compiles to ``redis.call('STRLEN', ...)``.

        https://redis.io/docs/latest/commands/strlen/
        """

    def subscribe(self, channel: Any, /, *args: Any) -> Any:
        """Listens for messages published to channels.

        Syntax::

            SUBSCRIBE channel [channel ...]

        Time complexity: O(N) where N is the number of channels to subscribe to.

        Available since Redis 2.0.0. Compiles to ``redis.call('SUBSCRIBE', ...)``.

        https://redis.io/docs/latest/commands/subscribe/
        """

    def substr(self, key: Any, start: Any, end: Any, /) -> Any:
        """Returns a substring from a string value.

        Deprecated since Redis 2.0.0, replaced by ``GETRANGE``.

        Syntax::

            SUBSTR key start end

        Reply: The substring of the string value stored at key, determined by the offsets start
        and end (both are inclusive).

        Time complexity: O(N) where N is the length of the returned string. The complexity is
        ultimately determined by the returned length, but because creating a substring from an
        existing string is very cheap, it can be considered O(1) for small strings.

        Available since Redis 1.0.0. Compiles to ``redis.call('SUBSTR', ...)``.

        https://redis.io/docs/latest/commands/substr/
        """

    def sunion(self, key: Any, /, *args: Any) -> Any:
        """Returns the union of multiple sets.

        Syntax::

            SUNION key [key ...]

        Reply: List with the members of the resulting set.

        Time complexity: O(N) where N is the total number of elements in all given sets.

        Available since Redis 1.0.0. Compiles to ``redis.call('SUNION', ...)``.

        https://redis.io/docs/latest/commands/sunion/
        """

    def sunioncard(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns the number of members of the union of multiple sets.

        Syntax::

            SUNIONCARD numkeys key [key ...] [APPROX] [LIMIT limit]

        Reply: Number of the elements in the resulting union.

        Time complexity: O(N) where N is the total number of elements in all given sets.

        Available since Redis 8.10.0. Compiles to ``redis.call('SUNIONCARD', ...)``.

        https://redis.io/docs/latest/commands/sunioncard/
        """

    def sunionstore(self, destination: Any, key: Any, /, *args: Any) -> Any:
        """Stores the union of multiple sets in a key.

        Syntax::

            SUNIONSTORE destination key [key ...]

        Reply: Number of the elements in the resulting set.

        Time complexity: O(N) where N is the total number of elements in all given sets.

        Available since Redis 1.0.0. Compiles to ``redis.call('SUNIONSTORE', ...)``.

        https://redis.io/docs/latest/commands/sunionstore/
        """

    def sunsubscribe(self, *args: Any) -> Any:
        """Stops listening to messages posted to shard channels.

        Syntax::

            SUNSUBSCRIBE [shardchannel [shardchannel ...]]

        Time complexity: O(N) where N is the number of shard channels to unsubscribe.

        Available since Redis 7.0.0. Compiles to ``redis.call('SUNSUBSCRIBE', ...)``.

        https://redis.io/docs/latest/commands/sunsubscribe/
        """

    def swapdb(self, index1: Any, index2: Any, /) -> Any:
        """Swaps two Redis databases.

        Syntax::

            SWAPDB index1 index2

        Time complexity: O(N) where N is the count of clients watching or blocking on keys from
        both databases.

        Available since Redis 4.0.0. Compiles to ``redis.call('SWAPDB', ...)``.

        https://redis.io/docs/latest/commands/swapdb/
        """

    def sync(self) -> Any:
        """An internal command used in replication.

        Syntax::

            SYNC

        Available since Redis 1.0.0. Compiles to ``redis.call('SYNC')``.

        https://redis.io/docs/latest/commands/sync/
        """

    def time(self) -> Any:
        """Returns the server time.

        Syntax::

            TIME

        Reply: Array containing two elements: Unix time in seconds and microseconds.

        Time complexity: O(1)

        Available since Redis 2.6.0. Compiles to ``redis.call('TIME')``.

        https://redis.io/docs/latest/commands/time/
        """

    def touch(self, key: Any, /, *args: Any) -> Any:
        """Returns the number of existing keys out of those specified after updating the time they
        were last accessed.

        Syntax::

            TOUCH key [key ...]

        Reply: the number of touched keys

        Time complexity: O(N) where N is the number of keys that will be touched.

        Available since Redis 3.2.1. Compiles to ``redis.call('TOUCH', ...)``.

        https://redis.io/docs/latest/commands/touch/
        """

    def trimslots(self, *args: Any) -> Any:
        """Trim the keys that belong to specified slots.

        Syntax::

            TRIMSLOTS RANGES numranges startslot endslot [startslot endslot ...]

        Time complexity: O(N) where N is the total number of keys in all databases

        Available since Redis 8.4.0. Compiles to ``redis.call('TRIMSLOTS', ...)``.

        https://redis.io/docs/latest/commands/trimslots/
        """

    def ttl(self, key: Any, /) -> Any:
        """Returns the expiration time in seconds of a key.

        Syntax::

            TTL key

        Reply, one of:

        - TTL in seconds.
        - The key exists but has no associated expire.
        - The key does not exist.

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('TTL', ...)``.

        https://redis.io/docs/latest/commands/ttl/
        """

    def type(self, key: Any, /) -> Any:
        """Determines the type of value stored at a key.

        Syntax::

            TYPE key

        Reply, one of:

        - Key doesn't exist
        - Type of the key

        Time complexity: O(1)

        Available since Redis 1.0.0. Compiles to ``redis.call('TYPE', ...)``.

        https://redis.io/docs/latest/commands/type/
        """

    def unlink(self, key: Any, /, *args: Any) -> Any:
        """Asynchronously deletes one or more keys.

        Syntax::

            UNLINK key [key ...]

        Reply: the number of keys that were unlinked

        Time complexity: O(1) for each key removed regardless of its size. Then the command does
        O(N) work in a different thread in order to reclaim memory, where N is the number of
        allocations the deleted objects where composed of.

        Available since Redis 4.0.0. Compiles to ``redis.call('UNLINK', ...)``.

        https://redis.io/docs/latest/commands/unlink/
        """

    def unsubscribe(self, *args: Any) -> Any:
        """Stops listening to messages posted to channels.

        Syntax::

            UNSUBSCRIBE [channel [channel ...]]

        Time complexity: O(N) where N is the number of channels to unsubscribe.

        Available since Redis 2.0.0. Compiles to ``redis.call('UNSUBSCRIBE', ...)``.

        https://redis.io/docs/latest/commands/unsubscribe/
        """

    def unwatch(self) -> Any:
        """Forgets about watched keys of a transaction.

        Syntax::

            UNWATCH

        Time complexity: O(1)

        Available since Redis 2.2.0. Compiles to ``redis.call('UNWATCH')``.

        https://redis.io/docs/latest/commands/unwatch/
        """

    def wait(self, numreplicas: Any, timeout: Any, /) -> Any:
        """Blocks until the asynchronous replication of all preceding write commands sent by the
        connection is completed.

        Syntax::

            WAIT numreplicas timeout

        Reply: The number of replicas reached by all the writes performed in the context of the
        current connection.

        Time complexity: O(1)

        Available since Redis 3.0.0. Compiles to ``redis.call('WAIT', ...)``.

        https://redis.io/docs/latest/commands/wait/
        """

    def waitaof(self, numlocal: Any, numreplicas: Any, timeout: Any, /) -> Any:
        """Blocks until all of the preceding write commands sent by the connection are written to
        the append-only file of the master and/or replicas.

        Syntax::

            WAITAOF numlocal numreplicas timeout

        Reply: Number of local and remote AOF files in sync.

        Time complexity: O(1)

        Available since Redis 7.2.0. Compiles to ``redis.call('WAITAOF', ...)``.

        https://redis.io/docs/latest/commands/waitaof/
        """

    def watch(self, key: Any, /, *args: Any) -> Any:
        """Monitors changes to keys to determine the execution of a transaction.

        Syntax::

            WATCH key [key ...]

        Time complexity: O(1) for every key.

        Available since Redis 2.2.0. Compiles to ``redis.call('WATCH', ...)``.

        https://redis.io/docs/latest/commands/watch/
        """

    def xack(self, key: Any, group: Any, id: Any, /, *args: Any) -> Any:
        """Returns the number of messages that were successfully acknowledged by the consumer group
        member of a stream.

        Syntax::

            XACK key group ID [ID ...]

        Reply: The command returns the number of messages successfully acknowledged. Certain
        message IDs may no longer be part of the PEL (for example because they have already been
        acknowledged), and XACK will not count them as successfully acknowledged.

        Time complexity: O(1) for each message ID processed.

        Available since Redis 5.0.0. Compiles to ``redis.call('XACK', ...)``.

        https://redis.io/docs/latest/commands/xack/
        """

    def xackdel(self, key: Any, group: Any, /, *args: Any) -> Any:
        """Acknowledges and deletes one or multiple messages for a stream consumer group.

        Syntax::

            XACKDEL key group [KEEPREF | DELREF | ACKED] IDS numids id [id ...]

        Reply: Array of results. Returns an array with -1 for each requested ID if the key does
        not exist.

        Time complexity: O(1) for each message ID processed.

        Available since Redis 8.2.0. Compiles to ``redis.call('XACKDEL', ...)``.

        https://redis.io/docs/latest/commands/xackdel/
        """

    def xadd(self, key: Any, /, *args: Any) -> Any:
        """Appends a new message to a stream. Creates the key if it doesn't exist.

        Syntax::

            XADD key [NOMKSTREAM] [KEEPREF | DELREF | ACKED] [IDMPAUTO pid | IDMP pid iid]
                [<MAXLEN | MINID> [= | ~] threshold [LIMIT count]] <* | id> field value [field
                value ...]

        Reply, one of:

        - The ID of the added entry. The ID is the one auto-generated if * is passed as ID
          argument, otherwise the command just returns the same ID specified by the user during
          insertion.
        - The NOMKSTREAM option is given and the key doesn't exist.

        Time complexity: O(1) when adding a new entry, O(N) when trimming where N being the
        number of entries evicted.

        Available since Redis 5.0.0. Compiles to ``redis.call('XADD', ...)``.

        https://redis.io/docs/latest/commands/xadd/
        """

    def xautoclaim(
        self, key: Any, group: Any, consumer: Any, min_idle_time: Any, start: Any, /, *args: Any
    ) -> Any:
        """Changes, or acquires, ownership of messages in a consumer group, as if the messages were
        delivered to as consumer group member.

        Syntax::

            XAUTOCLAIM key group consumer min-idle-time start [COUNT count] [JUSTID]

        Reply, one of:

        - Claimed stream entries (with data, if ``JUSTID`` was not given).
        - Claimed stream entries (without data, if ``JUSTID`` was given).

        Time complexity: O(1) if COUNT is small.

        Available since Redis 6.2.0. Compiles to ``redis.call('XAUTOCLAIM', ...)``.

        https://redis.io/docs/latest/commands/xautoclaim/
        """

    def xcfgset(self, key: Any, /, *args: Any) -> Any:
        """Sets the IDMP configuration parameters for a stream.

        Syntax::

            XCFGSET key [IDMP-DURATION idmp-duration] [IDMP-MAXSIZE idmp-maxsize]

        Time complexity: O(1)

        Available since Redis 8.6.0. Compiles to ``redis.call('XCFGSET', ...)``.

        https://redis.io/docs/latest/commands/xcfgset/
        """

    def xclaim(
        self, key: Any, group: Any, consumer: Any, min_idle_time: Any, id: Any, /, *args: Any
    ) -> Any:
        """Changes, or acquires, ownership of a message in a consumer group, as if the message was
        delivered a consumer group member.

        Syntax::

            XCLAIM key group consumer min-idle-time ID [ID ...] [IDLE ms] [TIME
                unix-time-milliseconds] [RETRYCOUNT count] [FORCE] [JUSTID] [LASTID lastid]

        Reply: Stream entries with IDs matching the specified range.

        Time complexity: O(log N) with N being the number of messages in the PEL of the consumer
        group.

        Available since Redis 5.0.0. Compiles to ``redis.call('XCLAIM', ...)``.

        https://redis.io/docs/latest/commands/xclaim/
        """

    def xdel(self, key: Any, id: Any, /, *args: Any) -> Any:
        """Returns the number of messages after removing them from a stream.

        Syntax::

            XDEL key ID [ID ...]

        Reply: The number of entries actually deleted

        Time complexity: O(1) for each single item to delete in the stream, regardless of the
        stream size.

        Available since Redis 5.0.0. Compiles to ``redis.call('XDEL', ...)``.

        https://redis.io/docs/latest/commands/xdel/
        """

    def xdelex(self, key: Any, /, *args: Any) -> Any:
        """Deletes one or multiple entries from the stream.

        Syntax::

            XDELEX key [KEEPREF | DELREF | ACKED] IDS numids id [id ...]

        Reply: Array of results. Returns an array with -1 for each requested ID if the key does
        not exist.

        Time complexity: O(1) for each single item to delete in the stream, regardless of the
        stream size.

        Available since Redis 8.2.0. Compiles to ``redis.call('XDELEX', ...)``.

        https://redis.io/docs/latest/commands/xdelex/
        """

    def xgroup_create(self, key: Any, group: Any, /, *args: Any) -> Any:
        """Creates a consumer group.

        Syntax::

            XGROUP CREATE key group <id | $> [MKSTREAM] [ENTRIESREAD entries-read]

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XGROUP', 'CREATE', ...)``.

        https://redis.io/docs/latest/commands/xgroup-create/
        """

    def xgroup_createconsumer(self, key: Any, group: Any, consumer: Any, /) -> Any:
        """Creates a consumer in a consumer group.

        Syntax::

            XGROUP CREATECONSUMER key group consumer

        Reply: The number of created consumers (0 or 1)

        Time complexity: O(1)

        Available since Redis 6.2.0. Compiles to ``redis.call('XGROUP', 'CREATECONSUMER',
        ...)``.

        https://redis.io/docs/latest/commands/xgroup-createconsumer/
        """

    def xgroup_delconsumer(self, key: Any, group: Any, consumer: Any, /) -> Any:
        """Deletes a consumer from a consumer group.

        Syntax::

            XGROUP DELCONSUMER key group consumer

        Reply: The number of pending messages that were yet associated with such a consumer

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XGROUP', 'DELCONSUMER', ...)``.

        https://redis.io/docs/latest/commands/xgroup-delconsumer/
        """

    def xgroup_destroy(self, key: Any, group: Any, /) -> Any:
        """Destroys a consumer group.

        Syntax::

            XGROUP DESTROY key group

        Reply: The number of destroyed consumer groups (0 or 1)

        Time complexity: O(N) where N is the number of entries in the group's pending entries
        list (PEL).

        Available since Redis 5.0.0. Compiles to ``redis.call('XGROUP', 'DESTROY', ...)``.

        https://redis.io/docs/latest/commands/xgroup-destroy/
        """

    def xgroup_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            XGROUP HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XGROUP', 'HELP')``.

        https://redis.io/docs/latest/commands/xgroup-help/
        """

    def xgroup_setid(self, key: Any, group: Any, /, *args: Any) -> Any:
        """Sets the last-delivered ID of a consumer group.

        Syntax::

            XGROUP SETID key group <id | $> [ENTRIESREAD entries-read]

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XGROUP', 'SETID', ...)``.

        https://redis.io/docs/latest/commands/xgroup-setid/
        """

    def xidmprecord(self, key: Any, pid: Any, iid: Any, stream_id: Any, /) -> Any:
        """An internal command for setting IDMP metadata on an existing stream message.

        Syntax::

            XIDMPRECORD key pid iid stream-id

        Time complexity: O(1)

        Available since Redis 8.6.2. Compiles to ``redis.call('XIDMPRECORD', ...)``.

        https://redis.io/docs/latest/commands/xidmprecord/
        """

    def xinfo_consumers(self, key: Any, group: Any, /) -> Any:
        """Returns a list of the consumers in a consumer group.

        Syntax::

            XINFO CONSUMERS key group

        Reply: Array list of consumers

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XINFO', 'CONSUMERS', ...)``.

        https://redis.io/docs/latest/commands/xinfo-consumers/
        """

    def xinfo_groups(self, key: Any, /) -> Any:
        """Returns a list of the consumer groups of a stream.

        Syntax::

            XINFO GROUPS key

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XINFO', 'GROUPS', ...)``.

        https://redis.io/docs/latest/commands/xinfo-groups/
        """

    def xinfo_help(self) -> Any:
        """Returns helpful text about the different subcommands.

        Syntax::

            XINFO HELP

        Reply: Helpful text about subcommands.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XINFO', 'HELP')``.

        https://redis.io/docs/latest/commands/xinfo-help/
        """

    def xinfo_stream(self, key: Any, /, *args: Any) -> Any:
        """Returns information about a stream.

        Syntax::

            XINFO STREAM key [FULL [COUNT count]]

        Reply, one of:

        - Summary form, in case ``FULL`` was not given.
        - Extended form, in case ``FULL`` was given.

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XINFO', 'STREAM', ...)``.

        https://redis.io/docs/latest/commands/xinfo-stream/
        """

    def xlen(self, key: Any, /) -> Any:
        """Return the number of messages in a stream.

        Syntax::

            XLEN key

        Reply: The number of entries of the stream at key

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XLEN', ...)``.

        https://redis.io/docs/latest/commands/xlen/
        """

    def xnack(self, key: Any, group: Any, /, *args: Any) -> Any:
        """Releases claimed messages back to the group's PEL without acknowledging them, making
        them available for re-delivery.

        Syntax::

            XNACK key group <SILENT | FAIL | FATAL> IDS numids id [id ...] [RETRYCOUNT count]
                [FORCE]

        Reply: The number of messages successfully NACKed (released back to the group PEL).

        Time complexity: O(1) for each message ID processed.

        Available since Redis 8.8.0. Compiles to ``redis.call('XNACK', ...)``.

        https://redis.io/docs/latest/commands/xnack/
        """

    def xpending(self, key: Any, group: Any, /, *args: Any) -> Any:
        """Returns the information and entries from a stream consumer group's pending entries list.

        Syntax::

            XPENDING key group [[IDLE min-idle-time] start end count [consumer]]

        Reply, one of:

        - Extended form, in case ``start`` was given.
        - Summary form, in case ``start`` was not given.
        - Summary form, in case ``start`` was not given and there are no pending messages.

        Time complexity: O(N) with N being the number of elements returned, so asking for a
        small fixed number of entries per call is O(1). O(M), where M is the total number of
        entries scanned when used with the IDLE filter. When the command returns just the
        summary and the list of consumers is small, it runs in O(1) time; otherwise, an
        additional O(N) time for iterating every consumer.

        Available since Redis 5.0.0. Compiles to ``redis.call('XPENDING', ...)``.

        https://redis.io/docs/latest/commands/xpending/
        """

    def xrange(self, key: Any, start: Any, end: Any, /, *args: Any) -> Any:
        """Returns the messages from a stream within a range of IDs.

        Syntax::

            XRANGE key start end [COUNT count]

        Reply: Stream entries with IDs matching the specified range.

        Time complexity: O(N) with N being the number of elements being returned. If N is
        constant (e.g. always asking for the first 10 elements with COUNT), you can consider it
        O(1).

        Available since Redis 5.0.0. Compiles to ``redis.call('XRANGE', ...)``.

        https://redis.io/docs/latest/commands/xrange/
        """

    def xread(self, *args: Any) -> Any:
        """Returns messages from multiple streams with IDs greater than the ones requested. Blocks
        until a message is available otherwise.

        Syntax::

            XREAD [COUNT count] [MAXCOUNT maxcount] [MAXSIZE maxsize] [BLOCK milliseconds]
                STREAMS key [key ...] ID [ID ...]

        Reply, one of:

        - A map of key-value elements when each element composed of key name and the entries
          reported for that key
        - If BLOCK option is given, and a timeout occurs, or there is no stream we can serve

        Available since Redis 5.0.0. Compiles to ``redis.call('XREAD', ...)``.

        https://redis.io/docs/latest/commands/xread/
        """

    def xreadgroup(self, *args: Any) -> Any:
        """Returns new or historical messages from a stream for a consumer in a group. Blocks until
        a message is available otherwise.

        Syntax::

            XREADGROUP GROUP group consumer [COUNT count] [MAXCOUNT maxcount] [MAXSIZE maxsize]
                [BLOCK milliseconds] [CLAIM min-idle-time] [NOACK] STREAMS key [key ...] ID [ID
                ...]

        Reply, one of:

        - If BLOCK option is specified and the timeout expired
        - A map of key-value elements when each element composed of key name and the entries
          reported for that key

        Time complexity: For each stream mentioned: O(M) with M being the number of elements
        returned. If M is constant (e.g. always asking for the first 10 elements with COUNT),
        you can consider it O(1). On the other side when XREADGROUP blocks, XADD will pay the
        O(N) time in order to serve the N clients blocked on the stream getting new data.

        Available since Redis 5.0.0. Compiles to ``redis.call('XREADGROUP', ...)``.

        https://redis.io/docs/latest/commands/xreadgroup/
        """

    def xrevrange(self, key: Any, end: Any, start: Any, /, *args: Any) -> Any:
        """Returns the messages from a stream within a range of IDs in reverse order.

        Syntax::

            XREVRANGE key end start [COUNT count]

        Reply: An array of the entries with IDs matching the specified range

        Time complexity: O(N) with N being the number of elements returned. If N is constant
        (e.g. always asking for the first 10 elements with COUNT), you can consider it O(1).

        Available since Redis 5.0.0. Compiles to ``redis.call('XREVRANGE', ...)``.

        https://redis.io/docs/latest/commands/xrevrange/
        """

    def xsetid(self, key: Any, last_id: Any, /, *args: Any) -> Any:
        """An internal command for replicating stream values.

        Syntax::

            XSETID key last-id [ENTRIESADDED entries-added] [MAXDELETEDID max-deleted-id]

        Time complexity: O(1)

        Available since Redis 5.0.0. Compiles to ``redis.call('XSETID', ...)``.

        https://redis.io/docs/latest/commands/xsetid/
        """

    def xtrim(self, key: Any, /, *args: Any) -> Any:
        """Deletes messages from the beginning of a stream.

        Syntax::

            XTRIM key <MAXLEN | MINID> [= | ~] threshold [LIMIT count] [KEEPREF | DELREF |
                ACKED]

        Reply: The number of entries deleted from the stream.

        Time complexity: O(N), with N being the number of evicted entries. Constant times are
        very small however, since entries are organized in macro nodes containing multiple
        entries that can be released with a single deallocation.

        Available since Redis 5.0.0. Compiles to ``redis.call('XTRIM', ...)``.

        https://redis.io/docs/latest/commands/xtrim/
        """

    def zadd(self, key: Any, /, *args: Any) -> Any:
        """Adds one or more members to a sorted set, or updates their scores. Creates the key if it
        doesn't exist.

        Syntax::

            ZADD key [NX | XX] [GT | LT] [CH] [INCR] score member [score member ...]

        Reply, one of:

        - Operation was aborted (conflict with one of the ``XX``/``NX``/``LT``/``GT`` options).
        - The number of new members (when the ``CH`` option is not used)
        - The number of new or updated members (when the ``CH`` option is used)
        - The updated score of the member (when the ``INCR`` option is used)

        Time complexity: O(log(N)) for each item added, where N is the number of elements in the
        sorted set.

        Available since Redis 1.2.0. Compiles to ``redis.call('ZADD', ...)``.

        https://redis.io/docs/latest/commands/zadd/
        """

    def zcard(self, key: Any, /) -> Any:
        """Returns the number of members in a sorted set.

        Syntax::

            ZCARD key

        Reply: The cardinality (number of elements) of the sorted set, or 0 if key does not
        exist

        Time complexity: O(1)

        Available since Redis 1.2.0. Compiles to ``redis.call('ZCARD', ...)``.

        https://redis.io/docs/latest/commands/zcard/
        """

    def zcount(self, key: Any, min: Any, max: Any, /) -> Any:
        """Returns the count of members in a sorted set that have scores within a range.

        Syntax::

            ZCOUNT key min max

        Reply: The number of elements in the specified score range

        Time complexity: O(log(N)) with N being the number of elements in the sorted set.

        Available since Redis 2.0.0. Compiles to ``redis.call('ZCOUNT', ...)``.

        https://redis.io/docs/latest/commands/zcount/
        """

    def zdiff(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns the difference between multiple sorted sets.

        Syntax::

            ZDIFF numkeys key [key ...] [WITHSCORES]

        Reply, one of:

        - A list of members. Returned in case ``WITHSCORES`` was not used.
        - Members and their scores. Returned in case ``WITHSCORES`` was used. In RESP2 this is
          returned as a flat array

        Time complexity: O(L + (N-K)log(N)) worst case where L is the total number of elements
        in all the sets, N is the size of the first set, and K is the size of the result set.

        Available since Redis 6.2.0. Compiles to ``redis.call('ZDIFF', ...)``.

        https://redis.io/docs/latest/commands/zdiff/
        """

    def zdiffstore(self, destination: Any, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Stores the difference of multiple sorted sets in a key.

        Syntax::

            ZDIFFSTORE destination numkeys key [key ...]

        Reply: Number of elements in the resulting sorted set at ``destination``

        Time complexity: O(L + (N-K)log(N)) worst case where L is the total number of elements
        in all the sets, N is the size of the first set, and K is the size of the result set.

        Available since Redis 6.2.0. Compiles to ``redis.call('ZDIFFSTORE', ...)``.

        https://redis.io/docs/latest/commands/zdiffstore/
        """

    def zincrby(self, key: Any, increment: Any, member: Any, /) -> Any:
        """Increments the score of a member in a sorted set.

        Syntax::

            ZINCRBY key increment member

        Reply: The new score of ``member``

        Time complexity: O(log(N)) where N is the number of elements in the sorted set.

        Available since Redis 1.2.0. Compiles to ``redis.call('ZINCRBY', ...)``.

        https://redis.io/docs/latest/commands/zincrby/
        """

    def zinter(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns the intersect of multiple sorted sets.

        Syntax::

            ZINTER numkeys key [key ...] [WEIGHTS weight [weight ...]] [AGGREGATE <SUM | MIN |
                MAX | COUNT>] [WITHSCORES]

        Reply, one of:

        - Result of intersection, containing only the member names. Returned in case
          ``WITHSCORES`` was not used.
        - Result of intersection, containing members and their scores. Returned in case
          ``WITHSCORES`` was used. In RESP2 this is returned as a flat array

        Time complexity: O(N*K)+O(M*log(M)) worst case with N being the smallest input sorted
        set, K being the number of input sorted sets and M being the number of elements in the
        resulting sorted set.

        Available since Redis 6.2.0. Compiles to ``redis.call('ZINTER', ...)``.

        https://redis.io/docs/latest/commands/zinter/
        """

    def zintercard(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns the number of members of the intersect of multiple sorted sets.

        Syntax::

            ZINTERCARD numkeys key [key ...] [LIMIT limit]

        Reply: Number of elements in the resulting intersection.

        Time complexity: O(N*K) worst case with N being the smallest input sorted set, K being
        the number of input sorted sets.

        Available since Redis 7.0.0. Compiles to ``redis.call('ZINTERCARD', ...)``.

        https://redis.io/docs/latest/commands/zintercard/
        """

    def zinterstore(self, destination: Any, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Stores the intersect of multiple sorted sets in a key.

        Syntax::

            ZINTERSTORE destination numkeys key [key ...] [WEIGHTS weight [weight ...]]
                [AGGREGATE <SUM | MIN | MAX | COUNT>]

        Reply: Number of elements in the resulting sorted set.

        Time complexity: O(N*K)+O(M*log(M)) worst case with N being the smallest input sorted
        set, K being the number of input sorted sets and M being the number of elements in the
        resulting sorted set.

        Available since Redis 2.0.0. Compiles to ``redis.call('ZINTERSTORE', ...)``.

        https://redis.io/docs/latest/commands/zinterstore/
        """

    def zlexcount(self, key: Any, min: Any, max: Any, /) -> Any:
        """Returns the number of members in a sorted set within a lexicographical range.

        Syntax::

            ZLEXCOUNT key min max

        Reply: Number of elements in the specified score range.

        Time complexity: O(log(N)) with N being the number of elements in the sorted set.

        Available since Redis 2.8.9. Compiles to ``redis.call('ZLEXCOUNT', ...)``.

        https://redis.io/docs/latest/commands/zlexcount/
        """

    def zmpop(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns the highest- or lowest-scoring members from one or more sorted sets after
        removing them. Deletes the sorted set if the last member was popped.

        Syntax::

            ZMPOP numkeys key [key ...] <MIN | MAX> [COUNT count]

        Reply: No element could be popped.

        Time complexity: O(K) + O(M*log(N)) where K is the number of provided keys, N being the
        number of elements in the sorted set, and M being the number of elements popped.

        Available since Redis 7.0.0. Compiles to ``redis.call('ZMPOP', ...)``.

        https://redis.io/docs/latest/commands/zmpop/
        """

    def zmscore(self, key: Any, member: Any, /, *args: Any) -> Any:
        """Returns the score of one or more members in a sorted set.

        Syntax::

            ZMSCORE key member [member ...]

        Time complexity: O(N) where N is the number of members being requested.

        Available since Redis 6.2.0. Compiles to ``redis.call('ZMSCORE', ...)``.

        https://redis.io/docs/latest/commands/zmscore/
        """

    def zpopmax(self, key: Any, /, *args: Any) -> Any:
        """Returns the highest-scoring members from a sorted set after removing them. Deletes the
        sorted set if the last member was popped.

        Syntax::

            ZPOPMAX key [count]

        Reply, one of:

        - List of popped elements and scores when 'COUNT' isn't specified.
        - List of popped elements and scores when 'COUNT' is specified.

        Time complexity: O(log(N)*M) with N being the number of elements in the sorted set, and
        M being the number of elements popped.

        Available since Redis 5.0.0. Compiles to ``redis.call('ZPOPMAX', ...)``.

        https://redis.io/docs/latest/commands/zpopmax/
        """

    def zpopmin(self, key: Any, /, *args: Any) -> Any:
        """Returns the lowest-scoring members from a sorted set after removing them. Deletes the
        sorted set if the last member was popped.

        Syntax::

            ZPOPMIN key [count]

        Reply, one of:

        - List of popped elements and scores when 'COUNT' isn't specified.
        - List of popped elements and scores when 'COUNT' is specified.

        Time complexity: O(log(N)*M) with N being the number of elements in the sorted set, and
        M being the number of elements popped.

        Available since Redis 5.0.0. Compiles to ``redis.call('ZPOPMIN', ...)``.

        https://redis.io/docs/latest/commands/zpopmin/
        """

    def zrandmember(self, key: Any, /, *args: Any) -> Any:
        """Returns one or more random members from a sorted set.

        Syntax::

            ZRANDMEMBER key [count [WITHSCORES]]

        Reply, one of:

        - Key does not exist.
        - Randomly selected element when 'COUNT' is not used.
        - Randomly selected elements when 'COUNT' is used.
        - Randomly selected elements when 'COUNT' and 'WITHSCORES' modifiers are used.

        Time complexity: O(N) where N is the number of members returned

        Available since Redis 6.2.0. Compiles to ``redis.call('ZRANDMEMBER', ...)``.

        https://redis.io/docs/latest/commands/zrandmember/
        """

    def zrange(self, key: Any, start: Any, stop: Any, /, *args: Any) -> Any:
        """Returns members in a sorted set within a range of indexes.

        Syntax::

            ZRANGE key start stop [BYSCORE | BYLEX] [REV] [LIMIT offset count] [WITHSCORES]

        Reply, one of:

        - A list of member elements
        - Members and their scores. Returned in case ``WITHSCORES`` was used. In RESP2 this is
          returned as a flat array

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements returned.

        Available since Redis 1.2.0. Compiles to ``redis.call('ZRANGE', ...)``.

        https://redis.io/docs/latest/commands/zrange/
        """

    def zrangebylex(self, key: Any, min: Any, max: Any, /, *args: Any) -> Any:
        """Returns members in a sorted set within a lexicographical range.

        Deprecated since Redis 6.2.0, replaced by ``ZRANGE`` with the ``BYLEX`` argument.

        Syntax::

            ZRANGEBYLEX key min max [LIMIT offset count]

        Reply: List of elements in the specified score range.

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements being returned. If M is constant (e.g. always asking for the
        first 10 elements with LIMIT), you can consider it O(log(N)).

        Available since Redis 2.8.9. Compiles to ``redis.call('ZRANGEBYLEX', ...)``.

        https://redis.io/docs/latest/commands/zrangebylex/
        """

    def zrangebyscore(self, key: Any, min: Any, max: Any, /, *args: Any) -> Any:
        """Returns members in a sorted set within a range of scores.

        Deprecated since Redis 6.2.0, replaced by ``ZRANGE`` with the ``BYSCORE`` argument.

        Syntax::

            ZRANGEBYSCORE key min max [WITHSCORES] [LIMIT offset count]

        Reply, one of:

        - List of the elements in the specified score range, as not WITHSCORES
        - List of the elements and their scores in the specified score range, as WITHSCORES used

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements being returned. If M is constant (e.g. always asking for the
        first 10 elements with LIMIT), you can consider it O(log(N)).

        Available since Redis 1.0.5. Compiles to ``redis.call('ZRANGEBYSCORE', ...)``.

        https://redis.io/docs/latest/commands/zrangebyscore/
        """

    def zrangestore(self, dst: Any, src: Any, min: Any, max: Any, /, *args: Any) -> Any:
        """Stores a range of members from sorted set in a key.

        Syntax::

            ZRANGESTORE dst src min max [BYSCORE | BYLEX] [REV] [LIMIT offset count]

        Reply: Number of elements in the resulting sorted set.

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements stored into the destination key.

        Available since Redis 6.2.0. Compiles to ``redis.call('ZRANGESTORE', ...)``.

        https://redis.io/docs/latest/commands/zrangestore/
        """

    def zrank(self, key: Any, member: Any, /, *args: Any) -> Any:
        """Returns the index of a member in a sorted set ordered by ascending scores.

        Syntax::

            ZRANK key member [WITHSCORE]

        Reply, one of:

        - Key does not exist or the member does not exist in the sorted set.
        - The rank of the member when 'WITHSCORE' is not used.
        - The rank and score of the member when 'WITHSCORE' is used.

        Time complexity: O(log(N))

        Available since Redis 2.0.0. Compiles to ``redis.call('ZRANK', ...)``.

        https://redis.io/docs/latest/commands/zrank/
        """

    def zrem(self, key: Any, member: Any, /, *args: Any) -> Any:
        """Removes one or more members from a sorted set. Deletes the sorted set if all members
        were removed.

        Syntax::

            ZREM key member [member ...]

        Reply: The number of members removed from the sorted set, not including non existing
        members.

        Time complexity: O(M*log(N)) with N being the number of elements in the sorted set and M
        the number of elements to be removed.

        Available since Redis 1.2.0. Compiles to ``redis.call('ZREM', ...)``.

        https://redis.io/docs/latest/commands/zrem/
        """

    def zremrangebylex(self, key: Any, min: Any, max: Any, /) -> Any:
        """Removes members in a sorted set within a lexicographical range. Deletes the sorted set
        if all members were removed.

        Syntax::

            ZREMRANGEBYLEX key min max

        Reply: Number of elements removed.

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements removed by the operation.

        Available since Redis 2.8.9. Compiles to ``redis.call('ZREMRANGEBYLEX', ...)``.

        https://redis.io/docs/latest/commands/zremrangebylex/
        """

    def zremrangebyrank(self, key: Any, start: Any, stop: Any, /) -> Any:
        """Removes members in a sorted set within a range of indexes. Deletes the sorted set if all
        members were removed.

        Syntax::

            ZREMRANGEBYRANK key start stop

        Reply: Number of elements removed.

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements removed by the operation.

        Available since Redis 2.0.0. Compiles to ``redis.call('ZREMRANGEBYRANK', ...)``.

        https://redis.io/docs/latest/commands/zremrangebyrank/
        """

    def zremrangebyscore(self, key: Any, min: Any, max: Any, /) -> Any:
        """Removes members in a sorted set within a range of scores. Deletes the sorted set if all
        members were removed.

        Syntax::

            ZREMRANGEBYSCORE key min max

        Reply: Number of elements removed.

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements removed by the operation.

        Available since Redis 1.2.0. Compiles to ``redis.call('ZREMRANGEBYSCORE', ...)``.

        https://redis.io/docs/latest/commands/zremrangebyscore/
        """

    def zrevrange(self, key: Any, start: Any, stop: Any, /, *args: Any) -> Any:
        """Returns members in a sorted set within a range of indexes in reverse order.

        Deprecated since Redis 6.2.0, replaced by ``ZRANGE`` with the ``REV`` argument.

        Syntax::

            ZREVRANGE key start stop [WITHSCORES]

        Reply, one of:

        - List of member elements.
        - List of the members and their scores. Returned in case ``WITHSCORES`` was used.

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements returned.

        Available since Redis 1.2.0. Compiles to ``redis.call('ZREVRANGE', ...)``.

        https://redis.io/docs/latest/commands/zrevrange/
        """

    def zrevrangebylex(self, key: Any, max: Any, min: Any, /, *args: Any) -> Any:
        """Returns members in a sorted set within a lexicographical range in reverse order.

        Deprecated since Redis 6.2.0, replaced by ``ZRANGE`` with the ``REV`` and ``BYLEX``
        arguments.

        Syntax::

            ZREVRANGEBYLEX key max min [LIMIT offset count]

        Reply: List of the elements in the specified score range.

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements being returned. If M is constant (e.g. always asking for the
        first 10 elements with LIMIT), you can consider it O(log(N)).

        Available since Redis 2.8.9. Compiles to ``redis.call('ZREVRANGEBYLEX', ...)``.

        https://redis.io/docs/latest/commands/zrevrangebylex/
        """

    def zrevrangebyscore(self, key: Any, max: Any, min: Any, /, *args: Any) -> Any:
        """Returns members in a sorted set within a range of scores in reverse order.

        Deprecated since Redis 6.2.0, replaced by ``ZRANGE`` with the ``REV`` and ``BYSCORE``
        arguments.

        Syntax::

            ZREVRANGEBYSCORE key max min [WITHSCORES] [LIMIT offset count]

        Reply, one of:

        - List of the elements in the specified score range, as not WITHSCORES
        - List of the elements and their scores in the specified score range, as WITHSCORES used

        Time complexity: O(log(N)+M) with N being the number of elements in the sorted set and M
        the number of elements being returned. If M is constant (e.g. always asking for the
        first 10 elements with LIMIT), you can consider it O(log(N)).

        Available since Redis 2.2.0. Compiles to ``redis.call('ZREVRANGEBYSCORE', ...)``.

        https://redis.io/docs/latest/commands/zrevrangebyscore/
        """

    def zrevrank(self, key: Any, member: Any, /, *args: Any) -> Any:
        """Returns the index of a member in a sorted set ordered by descending scores.

        Syntax::

            ZREVRANK key member [WITHSCORE]

        Reply, one of:

        - Key does not exist or the member does not exist in the sorted set.
        - The rank of the member when 'WITHSCORE' is not used.
        - The rank and score of the member when 'WITHSCORE' is used.

        Time complexity: O(log(N))

        Available since Redis 2.0.0. Compiles to ``redis.call('ZREVRANK', ...)``.

        https://redis.io/docs/latest/commands/zrevrank/
        """

    def zscan(self, key: Any, cursor: Any, /, *args: Any) -> Any:
        """Iterates over members and scores of a sorted set.

        Syntax::

            ZSCAN key cursor [MATCH pattern] [COUNT count]

        Reply: cursor and scan response in array form

        Time complexity: O(1) for every call. O(N) for a complete iteration, including enough
        command calls for the cursor to return back to 0. N is the number of elements inside the
        collection.

        Available since Redis 2.8.0. Compiles to ``redis.call('ZSCAN', ...)``.

        https://redis.io/docs/latest/commands/zscan/
        """

    def zscore(self, key: Any, member: Any, /) -> Any:
        """Returns the score of a member in a sorted set.

        Syntax::

            ZSCORE key member

        Reply, one of:

        - The score of the member (a double precision floating point number). In RESP2, this is
          returned as string.
        - Member does not exist in the sorted set, or key does not exist.

        Time complexity: O(1)

        Available since Redis 1.2.0. Compiles to ``redis.call('ZSCORE', ...)``.

        https://redis.io/docs/latest/commands/zscore/
        """

    def zunion(self, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Returns the union of multiple sorted sets.

        Syntax::

            ZUNION numkeys key [key ...] [WEIGHTS weight [weight ...]] [AGGREGATE <SUM | MIN |
                MAX | COUNT>] [WITHSCORES]

        Reply, one of:

        - The result of union when 'WITHSCORES' is not used.
        - The result of union when 'WITHSCORES' is used.

        Time complexity: O(N)+O(M*log(M)) with N being the sum of the sizes of the input sorted
        sets, and M being the number of elements in the resulting sorted set.

        Available since Redis 6.2.0. Compiles to ``redis.call('ZUNION', ...)``.

        https://redis.io/docs/latest/commands/zunion/
        """

    def zunionstore(self, destination: Any, numkeys: Any, key: Any, /, *args: Any) -> Any:
        """Stores the union of multiple sorted sets in a key.

        Syntax::

            ZUNIONSTORE destination numkeys key [key ...] [WEIGHTS weight [weight ...]]
                [AGGREGATE <SUM | MIN | MAX | COUNT>]

        Reply: The number of elements in the resulting sorted set.

        Time complexity: O(N)+O(M log(M)) with N being the sum of the sizes of the input sorted
        sets, and M being the number of elements in the resulting sorted set.

        Available since Redis 2.0.0. Compiles to ``redis.call('ZUNIONSTORE', ...)``.

        https://redis.io/docs/latest/commands/zunionstore/
        """
